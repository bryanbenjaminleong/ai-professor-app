{
  "id": "course-ai-engineer-bootcamp",
  "title": "AI Engineer Bootcamp",
  "description": "Master AI engineering from fundamentals to production deployment. Build production-ready AI systems with hands-on projects covering prompt engineering, LLMs, RAG, agents, function calling, orchestration, testing, and deployment.",
  "topic": "AI Engineering",
  "target_audience": "Developers",
  "difficulty": "Intermediate-Advanced",
  "prerequisites": [
    "Strong programming fundamentals (Python preferred)",
    "Basic understanding of APIs and REST",
    "Familiarity with command line tools",
    "Understanding of basic data structures"
  ],
  "weeks": [
    {
      "week": 1,
      "title": "Prompt Engineering Mastery",
      "description": "Master the art and science of prompt engineering. Learn techniques to get consistent, reliable outputs from LLMs.",
      "lessons": [
        {
          "title": "Foundations of Prompt Engineering",
          "content": "## Introduction to Prompt Engineering\n\nPrompt engineering is the practice of crafting inputs to language models to achieve desired outputs. It's the first skill every AI engineer needs.\n\n### Core Concepts\n\n**1. Context Windows**\n- Understanding token limits (4K, 8K, 32K, 128K+)\n- How context affects model behavior\n- Strategies for managing large contexts\n\n**2. Temperature and Sampling**\n- Temperature (0-2): Controls randomness\n  - Low (0-0.3): Deterministic, focused outputs\n  - Medium (0.4-0.7): Balanced creativity\n  - High (0.8+): Creative, varied responses\n- Top-p (nucleus sampling): Probability threshold\n- Top-k: Limit vocabulary choices\n\n**3. System vs User Messages**\n```\nSystem: You are an expert Python developer.\nUser: Write a function to sort a list.\n```\n\n### Prompting Patterns\n\n**Zero-shot prompting**\n```\nClassify this sentiment: \"I love this product!\"\n```\n\n**Few-shot prompting**\n```\nClassify the sentiment:\nText: \"Great service!\" → Positive\nText: \"Terrible experience\" → Negative\nText: \"I love this product!\" →\n```\n\n**Chain-of-thought**\n```\nLet's think step by step.\nFirst, I'll analyze...\nThen, I'll consider...\nFinally, I'll conclude...\n```\n\n### Hands-on Exercise\nBuild a prompt template system that can:\n1. Accept different personas\n2. Use examples dynamically\n3. Apply chain-of-thought when needed\n\n### Key Takeaways\n- Prompt engineering is iterative\n- Structure matters as much as content\n- Document what works\n- Test across different models"
        },
        {
          "title": "Advanced Prompting Techniques",
          "content": "## Advanced Prompting Strategies\n\n### Chain-of-Thought (CoT) Prompting\n\n**When to use**: Complex reasoning, math, logic puzzles\n\n```\nProblem: A store has 15 apples. They sell 7 and receive a delivery of 12.\nHow many apples do they have?\n\nLet's solve this step by step:\n1. Start with 15 apples\n2. Sell 7: 15 - 7 = 8 apples\n3. Receive 12: 8 + 12 = 20 apples\nAnswer: 20 apples\n```\n\n### Tree-of-Thought (ToT) Prompting\n\nExplore multiple reasoning paths:\n```\nGenerate 3 different approaches to solve this problem.\nFor each approach:\n1. Describe the strategy\n2. List pros and cons\n3. Estimate success probability\n\nThen, select the best approach and execute it.\n```\n\n### Self-Consistency\n\nGenerate multiple reasoning paths and vote:\n```\nSolve this problem 5 times independently.\nThen compare answers and select the most common one.\n```\n\n### Prompt Chaining\n\nBreak complex tasks into sequential prompts:\n\n```python\n# Step 1: Extract entities\nentities = llm.generate(\"Extract all people and places from: {text}\")\n\n# Step 2: Analyze relationships\nrelationships = llm.generate(f\"How are these entities related: {entities}\")\n\n# Step 3: Generate summary\nsummary = llm.generate(f\"Summarize relationships: {relationships}\")\n```\n\n### Constitutional AI / Self-Critique\n\n```\nGenerate a response.\n\nNow critique your response:\n- What assumptions did you make?\n- What could be improved?\n- Are there any errors?\n\nProvide an improved version based on your critique.\n```\n\n### Role-Playing and Personas\n\n```\nYou are a senior software architect with 20 years of experience.\nYou prioritize:\n- Scalability\n- Maintainability\n- Performance\n\nReview this code and provide feedback.\n```\n\n### Structured Output Prompts\n\nForce specific formats:\n```\nOutput a JSON object with this exact structure:\n{\n  \"summary\": \"string (max 50 chars)\",\n  \"sentiment\": \"positive\" | \"negative\" | \"neutral\",\n  \"confidence\": float (0-1),\n  \"key_entities\": [\"string\", ...]\n}\n```\n\n### Anti-Patterns to Avoid\n\n1. **Vague instructions**: \"Write something good\"\n2. **Conflicting directions**: \"Be concise but thorough\"\n3. **Assumed context**: Not providing necessary background\n4. **Over-constraining**: Too many rules\n\n### Hands-on Exercise\n\nBuild a prompt system that:\n1. Uses chain-of-thought for reasoning tasks\n2. Implements self-critique for quality\n3. Chains prompts for complex workflows\n4. Outputs structured JSON reliably"
        },
        {
          "title": "Prompt Patterns and Templates",
          "content": "## Reusable Prompt Patterns\n\n### Pattern Library\n\n#### 1. The Expert Pattern\n```\nYou are a [ROLE] with [N] years of experience in [DOMAIN].\nYour expertise includes:\n- [SKILL_1]\n- [SKILL_2]\n- [SKILL_3]\n\nWhen responding, you:\n1. [BEHAVIOR_1]\n2. [BEHAVIOR_2]\n3. [BEHAVIOR_3]\n\n[TASK]\n```\n\n#### 2. The Output Format Pattern\n```\nTask: [TASK_DESCRIPTION]\n\nOutput format:\n## Summary\n[Brief summary]\n\n## Details\n[Detailed explanation]\n\n## Code (if applicable)\n```[language]\n[code]\n```\n\n## Next Steps\n[Recommended actions]\n```\n\n#### 3. The Few-Shot Template Pattern\n```\nTask: [TASK]\n\nExamples:\nInput: [EXAMPLE_1_INPUT]\nOutput: [EXAMPLE_1_OUTPUT]\n\nInput: [EXAMPLE_2_INPUT]\nOutput: [EXAMPLE_2_OUTPUT]\n\nInput: [EXAMPLE_3_INPUT]\nOutput: [EXAMPLE_3_OUTPUT]\n\nNow process:\nInput: [ACTUAL_INPUT]\nOutput:\n```\n\n#### 4. The Validation Pattern\n```\n[PRIMARY_TASK]\n\nBefore finalizing, verify:\n✓ All requirements met\n✓ No contradictions\n✓ Appropriate length\n✓ Correct format\n\nIf issues found, revise and recheck.\n```\n\n### Building a Prompt Template System\n\n```python\nclass PromptTemplate:\n    def __init__(self, template: str, variables: list):\n        self.template = template\n        self.variables = variables\n    \n    def render(self, **kwargs) -> str:\n        # Validate all variables provided\n        missing = set(self.variables) - set(kwargs.keys())\n        if missing:\n            raise ValueError(f\"Missing variables: {missing}\")\n        \n        return self.template.format(**kwargs)\n\n# Usage\nanalysis_template = PromptTemplate(\n    template=\"\"\"Analyze the following {content_type}:\n{content}\n\nFocus on: {focus_areas}\nOutput format: {output_format}\"\"\",\n    variables=[\"content_type\", \"content\", \"focus_areas\", \"output_format\"]\n)\n\nprompt = analysis_template.render(\n    content_type=\"product review\",\n    content=review_text,\n    focus_areas=\"sentiment, key features, issues\",\n    output_format=\"JSON with sentiment and list fields\"\n)\n```\n\n### Version Control for Prompts\n\n```yaml\n# prompts/v1/classify_sentiment.yaml\nid: classify_sentiment\nversion: 1.2.0\nmodel: gpt-4\ntemperature: 0.3\n\nsystem: |\n  You are a sentiment analysis expert.\n  Classify text as positive, negative, or neutral.\n\nuser_template: |\n  Classify the sentiment of this text: {{text}}\n  \n  Provide:\n  1. Sentiment label\n  2. Confidence score (0-1)\n  3. Key phrases that influenced your decision\n\nmetadata:\n  author: team-ml\n  last_updated: 2024-01-15\n  performance_metrics:\n    accuracy: 0.94\n    latency_p95: 1.2s\n```\n\n### A/B Testing Prompts\n\n```python\nclass PromptExperiment:\n    def __init__(self, variants: dict):\n        self.variants = variants\n        self.results = {k: [] for k in variants}\n    \n    def run(self, input_data, variant_key: str):\n        prompt = self.variants[variant_key].render(**input_data)\n        response = llm.generate(prompt)\n        \n        # Track metrics\n        self.results[variant_key].append({\n            'input': input_data,\n            'output': response,\n            'timestamp': time.time()\n        })\n        \n        return response\n```\n\n### Best Practices\n\n1. **Document everything**: Version, purpose, expected behavior\n2. **Test systematically**: Use consistent test cases\n3. **Monitor in production**: Track performance over time\n4. **Iterate based on data**: Let metrics guide improvements\n5. **Share learnings**: Build team knowledge base\n\n### Hands-on Exercise\n\nCreate a prompt template system that:\n1. Supports versioning\n2. Enables A/B testing\n3. Logs all interactions\n4. Provides performance metrics\n5. Allows easy rollback"
        },
        {
          "title": "Week 1 Project: Prompt Engineering Toolkit",
          "content": "## Project: Build a Prompt Engineering Toolkit\n\n### Objective\nCreate a comprehensive toolkit for prompt engineering that includes templates, testing, and optimization features.\n\n### Requirements\n\n#### 1. Template Manager\n```python\nclass TemplateManager:\n    \"\"\"\n    Manage prompt templates with:\n    - CRUD operations\n    - Version control\n    - Variable validation\n    \"\"\"\n    \n    def create_template(self, id, template, variables, metadata): pass\n    def get_template(self, id, version=None): pass\n    def update_template(self, id, template): pass\n    def list_templates(self, tags=None): pass\n    def render(self, id, **variables): pass\n```\n\n#### 2. Prompt Tester\n```python\nclass PromptTester:\n    \"\"\"\n    Test prompts with:\n    - Test case management\n    - Batch testing\n    - Result comparison\n    \"\"\"\n    \n    def add_test_case(self, name, input_vars, expected_output): pass\n    def run_test(self, template_id, test_case): pass\n    def run_all_tests(self, template_id): pass\n    def compare_results(self, template_id_v1, template_id_v2): pass\n```\n\n#### 3. Experiment Runner\n```python\nclass ExperimentRunner:\n    \"\"\"\n    A/B test prompts:\n    - Variant management\n    - Traffic splitting\n    - Statistical analysis\n    \"\"\"\n    \n    def create_experiment(self, name, variants): pass\n    def assign_variant(self, user_id): pass\n    def record_result(self, experiment, variant, result): pass\n    def analyze_results(self, experiment): pass\n```\n\n#### 4. Prompt Optimizer\n```python\nclass PromptOptimizer:\n    \"\"\"\n    Automatically optimize prompts:\n    - Generate variations\n    - Test variations\n    - Select best performer\n    \"\"\"\n    \n    def generate_variations(self, base_prompt, n=5): pass\n    def evaluate_variation(self, variation, test_cases): pass\n    def find_optimal(self, base_prompt, metric='accuracy'): pass\n```\n\n### Project Structure\n\n```\nprompt_toolkit/\n├── templates/\n│   ├── __init__.py\n│   ├── manager.py\n│   └── schema.py\n├── testing/\n│   ├── __init__.py\n│   ├── test_runner.py\n│   └── assertions.py\n├── experiments/\n│   ├── __init__.py\n│   ├── runner.py\n│   └── analytics.py\n├── optimization/\n│   ├── __init__.py\n│   ├── generator.py\n│   └── evaluator.py\n├── utils/\n│   ├── __init__.py\n│   ├── llm_client.py\n│   └── logger.py\n└── cli.py\n```\n\n### Deliverables\n\n1. **Working toolkit** with all components\n2. **Documentation** for each module\n3. **Example templates** for common use cases:\n   - Text classification\n   - Entity extraction\n   - Summarization\n   - Code generation\n   - Question answering\n4. **Test suite** with >80% coverage\n5. **CLI tool** for quick operations:\n   ```bash\n   prompt-toolkit render template_id --var key=value\n   prompt-toolkit test template_id --cases test_cases.json\n   prompt-toolkit experiment create --config exp.yaml\n   ```\n\n### Evaluation Criteria\n\n- Code quality and organization (25%)\n- Feature completeness (25%)\n- Documentation quality (20%)\n- Test coverage (15%)\n- Usability and CLI design (15%)\n\n### Extension Ideas\n\n- Web UI for template management\n- Integration with popular LLM providers\n- Export/import to various formats\n- Collaborative features (sharing, comments)\n- Performance monitoring dashboard"
        }
      ]
    },
    {
      "week": 2,
      "title": "LLM Fundamentals & APIs",
      "description": "Deep dive into Large Language Models - how they work, their capabilities, limitations, and how to use them via APIs.",
      "lessons": [
        {
          "title": "Understanding Large Language Models",
          "content": "## LLM Architecture and Capabilities\n\n### How LLMs Work\n\n#### Transformer Architecture\n\n```\nInput → Tokenization → Embedding → \n[Attention Layers × N] → \nOutput Probabilities → Detokenization → Text\n```\n\n**Key Components:**\n1. **Tokenization**: Text → tokens (words/subwords)\n2. **Embeddings**: Tokens → dense vectors\n3. **Self-Attention**: Understanding relationships\n4. **Feed-Forward Networks**: Processing information\n5. **Layer Normalization**: Stabilizing training\n\n### Model Families\n\n#### OpenAI GPT Series\n- GPT-3.5: Fast, cost-effective\n- GPT-4: Most capable, reasoning\n- GPT-4 Turbo: Faster, cheaper GPT-4\n\n#### Anthropic Claude\n- Claude 3 Haiku: Fast, efficient\n- Claude 3 Sonnet: Balanced\n- Claude 3 Opus: Most capable\n\n#### Open Source Models\n- Llama 2/3 (Meta): Strong open models\n- Mistral/Mixtral: Efficient, capable\n- Gemma (Google): Lightweight\n- Falcon: High performance\n\n### Capabilities\n\n**Strengths:**\n- Text generation and completion\n- Code generation and explanation\n- Translation and summarization\n- Question answering\n- Creative writing\n- Data extraction and formatting\n\n**Limitations:**\n- No real-time information (without tools)\n- Can hallucinate facts\n- Limited context despite large windows\n- No persistent memory\n- Mathematical reasoning can be inconsistent\n\n### Token Economics\n\n**Understanding Tokens:**\n- 1 token ≈ 4 characters (English)\n- 1 token ≈ 0.75 words\n- Code uses more tokens\n\n**Cost Calculation:**\n```\nCost = (Input tokens × Input price) + (Output tokens × Output price)\n\nExample (GPT-4 Turbo):\nInput: 1,000 tokens × $0.01/1K = $0.01\nOutput: 500 tokens × $0.03/1K = $0.015\nTotal: $0.025\n```\n\n### Context Window Strategies\n\n#### Chunking for Long Documents\n```python\ndef chunk_text(text, max_tokens=4000, overlap=200):\n    \"\"\"\n    Split text into overlapping chunks.\n    \"\"\"\n    tokens = tokenize(text)\n    chunks = []\n    \n    for i in range(0, len(tokens), max_tokens - overlap):\n        chunk = tokens[i:i + max_tokens]\n        chunks.append(detokenize(chunk))\n    \n    return chunks\n```\n\n#### Sliding Window Processing\n```python\ndef process_with_sliding_window(document, prompt, window_size=3000, stride=2000):\n    results = []\n    \n    for i in range(0, len(document), stride):\n        window = document[i:i+window_size]\n        result = llm.generate(f\"{prompt}\\n\\n{window}\")\n        results.append(result)\n    \n    return combine_results(results)\n```\n\n### Model Selection Guide\n\n| Use Case | Recommended Model | Why |\n|----------|------------------|-----|\n| Simple tasks, high volume | GPT-3.5 / Haiku | Cost-effective |\n| Complex reasoning | GPT-4 / Opus | Best reasoning |\n| Code generation | GPT-4 / Claude | Strong code understanding |\n| Long context (>50K) | Claude 3 | 200K context |\n| Self-hosted | Llama 3 / Mixtral | No API dependency |\n\n### Hands-on Exercise\n\n1. Compare outputs from 3 different models on the same task\n2. Measure token usage and costs\n3. Test hallucination rates with fact-checking\n4. Implement a model router that selects models based on task type"
        },
        {
          "title": "Working with LLM APIs",
          "content": "## LLM API Integration\n\n### OpenAI API\n\n#### Setup\n```bash\npip install openai\n```\n\n```python\nfrom openai import OpenAI\n\nclient = OpenAI(api_key=\"your-api-key\")\n```\n\n#### Basic Completion\n```python\nresponse = client.chat.completions.create(\n    model=\"gpt-4-turbo-preview\",\n    messages=[\n        {\"role\": \"system\", \"content\": \"You are a helpful assistant.\"},\n        {\"role\": \"user\", \"content\": \"Explain quantum computing.\"}\n    ],\n    temperature=0.7,\n    max_tokens=500\n)\n\nprint(response.choices[0].message.content)\n```\n\n#### Streaming Responses\n```python\nstream = client.chat.completions.create(\n    model=\"gpt-4-turbo-preview\",\n    messages=[{\"role\": \"user\", \"content\": \"Write a story.\"}],\n    stream=True\n)\n\nfor chunk in stream:\n    if chunk.choices[0].delta.content:\n        print(chunk.choices[0].delta.content, end=\"\")\n```\n\n#### Function Calling\n```python\nresponse = client.chat.completions.create(\n    model=\"gpt-4-turbo-preview\",\n    messages=[{\"role\": \"user\", \"content\": \"What's the weather in Tokyo?\"}],\n    tools=[{\n        \"type\": \"function\",\n        \"function\": {\n            \"name\": \"get_weather\",\n            \"description\": \"Get weather for a city\",\n            \"parameters\": {\n                \"type\": \"object\",\n                \"properties\": {\n                    \"city\": {\"type\": \"string\"}\n                },\n                \"required\": [\"city\"]\n            }\n        }\n    }]\n)\n\nif response.choices[0].message.tool_calls:\n    tool_call = response.choices[0].message.tool_calls[0]\n    # Execute function and continue...\n```\n\n### Anthropic Claude API\n\n```python\nimport anthropic\n\nclient = anthropic.Anthropic(api_key=\"your-api-key\")\n\nmessage = client.messages.create(\n    model=\"claude-3-opus-20240229\",\n    max_tokens=1024,\n    system=\"You are a helpful assistant.\",\n    messages=[\n        {\"role\": \"user\", \"content\": \"Explain machine learning.\"}\n    ]\n)\n\nprint(message.content[0].text)\n```\n\n### Building an LLM Client Wrapper\n\n```python\nclass UnifiedLLMClient:\n    \"\"\"\n    Unified interface for multiple LLM providers.\n    \"\"\"\n    \n    def __init__(self, provider=\"openai\", model=\"gpt-4\"):\n        self.provider = provider\n        self.model = model\n        self.client = self._init_client()\n    \n    def _init_client(self):\n        if self.provider == \"openai\":\n            return OpenAI()\n        elif self.provider == \"anthropic\":\n            return anthropic.Anthropic()\n        else:\n            raise ValueError(f\"Unknown provider: {self.provider}\")\n    \n    def generate(\n        self,\n        prompt: str,\n        system: str = None,\n        temperature: float = 0.7,\n        max_tokens: int = 1000,\n        stream: bool = False\n    ):\n        if self.provider == \"openai\":\n            messages = []\n            if system:\n                messages.append({\"role\": \"system\", \"content\": system})\n            messages.append({\"role\": \"user\", \"content\": prompt})\n            \n            response = self.client.chat.completions.create(\n                model=self.model,\n                messages=messages,\n                temperature=temperature,\n                max_tokens=max_tokens,\n                stream=stream\n            )\n            \n            if stream:\n                return self._stream_openai(response)\n            return response.choices[0].message.content\n        \n        elif self.provider == \"anthropic\":\n            response = self.client.messages.create(\n                model=self.model,\n                max_tokens=max_tokens,\n                system=system,\n                messages=[{\"role\": \"user\", \"content\": prompt}],\n                temperature=temperature\n            )\n            return response.content[0].text\n    \n    def _stream_openai(self, stream):\n        for chunk in stream:\n            if chunk.choices[0].delta.content:\n                yield chunk.choices[0].delta.content\n```\n\n### Error Handling and Retries\n\n```python\nfrom tenacity import retry, stop_after_attempt, wait_exponential\nimport openai\n\n@retry(\n    stop=stop_after_attempt(3),\n    wait=wait_exponential(multiplier=1, min=4, max=10),\n    retry=lambda e: isinstance(e, openai.RateLimitError)\n)\ndef generate_with_retry(prompt: str):\n    try:\n        return client.chat.completions.create(\n            model=\"gpt-4\",\n            messages=[{\"role\": \"user\", \"content\": prompt}]\n        )\n    except openai.APIConnectionError as e:\n        logger.error(f\"Connection error: {e}\")\n        raise\n    except openai.RateLimitError as e:\n        logger.warning(f\"Rate limit hit, retrying...\")\n        raise\n    except openai.APIStatusError as e:\n        logger.error(f\"API error: {e}\")\n        raise\n```\n\n### Rate Limiting and Cost Management\n\n```python\nclass RateLimitedLLM:\n    def __init__(self, rpm_limit=60, token_limit=90000):\n        self.rpm_limit = rpm_limit\n        self.token_limit = token_limit\n        self.request_times = []\n        self.token_usage = []\n    \n    def generate(self, prompt, **kwargs):\n        self._wait_if_needed()\n        \n        response = llm.generate(prompt, **kwargs)\n        \n        self.request_times.append(time.time())\n        self.token_usage.append(response.usage.total_tokens)\n        \n        return response\n    \n    def _wait_if_needed(self):\n        now = time.time()\n        \n        # Clean old requests\n        self.request_times = [t for t in self.request_times if now - t < 60]\n        \n        if len(self.request_times) >= self.rpm_limit:\n            wait_time = 60 - (now - self.request_times[0])\n            time.sleep(wait_time)\n```\n\n### Hands-on Exercise\n\n1. Build a unified LLM client supporting 3 providers\n2. Implement streaming with proper error handling\n3. Add rate limiting and cost tracking\n4. Create a simple CLI tool for testing"
        },
        {
          "title": "Token Management and Optimization",
          "content": "## Token Optimization Strategies\n\n### Understanding Tokenization\n\n```python\nimport tiktoken\n\ndef count_tokens(text: str, model: str = \"gpt-4\") -> int:\n    \"\"\"Count tokens in text.\"\"\"\n    encoding = tiktoken.encoding_for_model(model)\n    return len(encoding.encode(text))\n\ndef tokenize(text: str, model: str = \"gpt-4\") -> list:\n    \"\"\"Convert text to tokens.\"\"\"\n    encoding = tiktoken.encoding_for_model(model)\n    return encoding.encode(text)\n\ndef detokenize(tokens: list, model: str = \"gpt-4\") -> str:\n    \"\"\"Convert tokens back to text.\"\"\"\n    encoding = tiktoken.encoding_for_model(model)\n    return encoding.decode(tokens)\n```\n\n### Reducing Token Usage\n\n#### 1. Compress Prompts\n```python\ndef compress_prompt(prompt: str, max_tokens: int) -> str:\n    \"\"\"Compress prompt while preserving meaning.\"\"\"\n    current_tokens = count_tokens(prompt)\n    \n    if current_tokens <= max_tokens:\n        return prompt\n    \n    # Strategy 1: Remove redundant phrases\n    prompt = re.sub(r'\\b(very|really|quite|extremely)\\b', '', prompt)\n    \n    # Strategy 2: Shorten examples\n    # Keep structure but condense content\n    \n    # Strategy 3: Use abbreviations\n    replacements = {\n        'for example': 'e.g.',\n        'that is': 'i.e.',\n        'and so on': 'etc.',\n        'please': ''\n    }\n    \n    for old, new in replacements.items():\n        prompt = prompt.replace(old, new)\n    \n    return prompt\n```\n\n#### 2. Dynamic Few-Shot Selection\n```python\ndef select_relevant_examples(query: str, examples: list, k: int = 3):\n    \"\"\"Select most relevant examples using similarity.\"\"\"\n    query_embedding = embed(query)\n    \n    scored_examples = []\n    for example in examples:\n        score = cosine_similarity(query_embedding, embed(example['input']))\n        scored_examples.append((score, example))\n    \n    scored_examples.sort(reverse=True)\n    return [ex for _, ex in scored_examples[:k]]\n\n# Usage\nexamples = load_examples('training_data.json')\nrelevant = select_relevant_examples(user_query, examples, k=3)\nprompt = build_few_shot_prompt(user_query, relevant)\n```\n\n#### 3. Caching Responses\n```python\nimport hashlib\nfrom functools import lru_cache\n\nclass ResponseCache:\n    def __init__(self, ttl_seconds=3600):\n        self.cache = {}\n        self.ttl = ttl_seconds\n    \n    def _hash_request(self, model: str, messages: list, temperature: float):\n        content = json.dumps({\n            'model': model,\n            'messages': messages,\n            'temperature': temperature\n        }, sort_keys=True)\n        return hashlib.sha256(content.encode()).hexdigest()\n    \n    def get(self, model, messages, temperature):\n        key = self._hash_request(model, messages, temperature)\n        if key in self.cache:\n            entry = self.cache[key]\n            if time.time() - entry['timestamp'] < self.ttl:\n                return entry['response']\n        return None\n    \n    def set(self, model, messages, temperature, response):\n        key = self._hash_request(model, messages, temperature)\n        self.cache[key] = {\n            'response': response,\n            'timestamp': time.time()\n        }\n\n# Usage\ncache = ResponseCache()\n\ndef generate_cached(prompt, model=\"gpt-4\", temperature=0.7):\n    messages = [{\"role\": \"user\", \"content\": prompt}]\n    \n    cached = cache.get(model, messages, temperature)\n    if cached:\n        return cached\n    \n    response = client.chat.completions.create(\n        model=model,\n        messages=messages,\n        temperature=temperature\n    )\n    \n    result = response.choices[0].message.content\n    cache.set(model, messages, temperature, result)\n    return result\n```\n\n### Token Budget Management\n\n```python\nclass TokenBudget:\n    def __init__(self, max_tokens: int):\n        self.max_tokens = max_tokens\n        self.used_tokens = 0\n        self.allocations = {}\n    \n    def allocate(self, name: str, tokens: int) -> bool:\n        \"\"\"Allocate tokens for a component.\"\"\"\n        if self.used_tokens + tokens > self.max_tokens:\n            return False\n        \n        self.allocations[name] = tokens\n        self.used_tokens += tokens\n        return True\n    \n    def remaining(self) -> int:\n        return self.max_tokens - self.used_tokens\n    \n    def optimize_allocation(self, components: dict):\n        \"\"\"Distribute tokens optimally across components.\"\"\"\n        total_requested = sum(components.values())\n        \n        if total_requested <= self.max_tokens:\n            for name, tokens in components.items():\n                self.allocate(name, tokens)\n            return\n        \n        # Scale down proportionally\n        scale_factor = self.max_tokens / total_requested\n        for name, tokens in components.items():\n            self.allocate(name, int(tokens * scale_factor))\n\n# Usage\nbudget = TokenBudget(max_tokens=4096)\nbudget.optimize_allocation({\n    'system_prompt': 500,\n    'examples': 1500,\n    'user_input': 1000,\n    'output_reserve': 1096\n})\n```\n\n### Monitoring Token Usage\n\n```python\nclass TokenMonitor:\n    def __init__(self):\n        self.usage_log = []\n    \n    def log_usage(self, request_id, model, input_tokens, output_tokens, cost):\n        self.usage_log.append({\n            'request_id': request_id,\n            'timestamp': datetime.now(),\n            'model': model,\n            'input_tokens': input_tokens,\n            'output_tokens': output_tokens,\n            'total_tokens': input_tokens + output_tokens,\n            'cost': cost\n        })\n    \n    def get_stats(self, period='day'):\n        \"\"\"Get usage statistics.\"\"\"\n        now = datetime.now()\n        \n        if period == 'day':\n            start = now - timedelta(days=1)\n        elif period == 'week':\n            start = now - timedelta(weeks=1)\n        \n        relevant = [l for l in self.usage_log if l['timestamp'] > start]\n        \n        return {\n            'requests': len(relevant),\n            'total_tokens': sum(l['total_tokens'] for l in relevant),\n            'total_cost': sum(l['cost'] for l in relevant),\n            'by_model': self._group_by_model(relevant)\n        }\n```\n\n### Hands-on Exercise\n\n1. Build a token counter for multiple models\n2. Implement smart prompt compression\n3. Create a caching layer with semantic matching\n4. Build a dashboard for token usage monitoring"
        },
        {
          "title": "Week 2 Project: Multi-Provider LLM Gateway",
          "content": "## Project: Build an LLM Gateway\n\n### Objective\nCreate a production-ready LLM gateway that abstracts multiple providers, handles failures, optimizes costs, and provides observability.\n\n### Requirements\n\n#### 1. Multi-Provider Support\n```python\nclass LLMGateway:\n    \"\"\"\n    Gateway supporting multiple LLM providers.\n    \"\"\"\n    \n    def __init__(self, config: dict):\n        self.providers = {\n            'openai': OpenAIProvider(config['openai']),\n            'anthropic': AnthropicProvider(config['anthropic']),\n            'local': LocalProvider(config['local'])\n        }\n        self.router = ModelRouter()\n        self.fallback_chain = ['openai', 'anthropic', 'local']\n    \n    async def generate(\n        self,\n        prompt: str,\n        model: str = None,\n        provider: str = None,\n        **kwargs\n    ) -> GenerationResult:\n        \"\"\"Generate with automatic provider selection.\"\"\"\n        pass\n```\n\n#### 2. Intelligent Routing\n```python\nclass ModelRouter:\n    \"\"\"\n    Route requests to optimal model/provider.\n    \"\"\"\n    \n    def select_model(self, request: Request) -> ModelSelection:\n        \"\"\"\n        Select best model based on:\n        - Task type (reasoning, coding, creative)\n        - Required capabilities (function calling, vision)\n        - Cost constraints\n        - Latency requirements\n        - Current load\n        \"\"\"\n        pass\n    \n    def route_by_cost(self, request: Request) -> str:\n        \"\"\"Route to cheapest capable model.\"\"\"\n        pass\n    \n    def route_by_latency(self, request: Request) -> str:\n        \"\"\"Route to fastest model.\"\"\"\n        pass\n```\n\n#### 3. Fallback and Retry\n```python\nclass FallbackHandler:\n    \"\"\"\n    Handle failures with fallback logic.\n    \"\"\"\n    \n    async def execute_with_fallback(\n        self,\n        request: Request,\n        fallback_chain: list\n    ) -> Result:\n        \"\"\"\n        Try providers in order until success.\n        \"\"\"\n        pass\n    \n    def should_retry(self, error: Exception) -> bool:\n        \"\"\"Determine if error is retryable.\"\"\"\n        pass\n```\n\n#### 4. Cost Optimization\n```python\nclass CostOptimizer:\n    \"\"\"\n    Optimize API costs.\n    \"\"\"\n    \n    def should_cache(self, request: Request) -> bool:\n        \"\"\"Determine if request should be cached.\"\"\"\n        pass\n    \n    def compress_prompt(self, prompt: str) -> str:\n        \"\"\"Reduce token count while preserving meaning.\"\"\"\n        pass\n    \n    def select_cheaper_model(self, request: Request) -> str:\n        \"\"\"Select cheapest model that meets requirements.\"\"\"\n        pass\n```\n\n#### 5. Observability\n```python\nclass Observability:\n    \"\"\"\n    Track metrics, logs, and traces.\n    \"\"\"\n    \n    def log_request(self, request: Request, response: Response):\n        \"\"\"Log all request details.\"\"\"\n        pass\n    \n    def track_metrics(self, metric: str, value: float, tags: dict):\n        \"\"\"Track custom metrics.\"\"\"\n        pass\n    \n    def get_trace(self, request_id: str) -> Trace:\n        \"\"\"Get full request trace.\"\"\"\n        pass\n```\n\n### Project Structure\n\n```\nllm_gateway/\n├── core/\n│   ├── __init__.py\n│   ├── gateway.py\n│   └── types.py\n├── providers/\n│   ├── __init__.py\n│   ├── base.py\n│   ├── openai_provider.py\n│   ├── anthropic_provider.py\n│   └── local_provider.py\n├── routing/\n│   ├── __init__.py\n│   ├── router.py\n│   └── policies.py\n├── resilience/\n│   ├── __init__.py\n│   ├── fallback.py\n│   ├── retry.py\n│   └── circuit_breaker.py\n├── optimization/\n│   ├── __init__.py\n│   ├── cache.py\n│   ├── compression.py\n│   └── cost.py\n├── observability/\n│   ├── __init__.py\n│   ├── metrics.py\n│   ├── logging.py\n│   └── tracing.py\n├── api/\n│   ├── __init__.py\n│   ├── routes.py\n│   └── schemas.py\n└── config/\n    ├── __init__.py\n    └── settings.py\n```\n\n### API Endpoints\n\n```python\n# REST API\nPOST /v1/generate\n{\n  \"prompt\": \"string\",\n  \"model\": \"string?\",\n  \"provider\": \"string?\",\n  \"temperature\": 0.7,\n  \"max_tokens\": 1000,\n  \"routing\": {\n    \"strategy\": \"cost|latency|quality\",\n    \"fallback\": true\n  }\n}\n\nPOST /v1/stream\n{\n  \"prompt\": \"string\",\n  ...\n}\n\nGET /v1/models\n[\n  {\"id\": \"gpt-4\", \"provider\": \"openai\", \"capabilities\": [...]},\n  ...\n]\n\nGET /v1/usage\n{\n  \"total_tokens\": 1000000,\n  \"total_cost\": 50.00,\n  \"by_model\": {...}\n}\n```\n\n### Deliverables\n\n1. **Working gateway** with all features\n2. **Provider implementations** for OpenAI, Anthropic, and local models\n3. **CLI tool** for testing:\n   ```bash\n   llm-gateway generate \"Hello, world!\" --model gpt-4\n   llm-gateway stream \"Tell me a story\" --provider anthropic\n   llm-gateway usage --period week\n   ```\n4. **Docker setup** for easy deployment\n5. **Comprehensive tests** with mocked providers\n6. **Documentation** for API and configuration\n\n### Evaluation Criteria\n\n- Code quality (20%)\n- Feature completeness (25%)\n- Error handling and resilience (20%)\n- Observability (15%)\n- Documentation (10%)\n- Tests (10%)\n\n### Extension Ideas\n\n- GraphQL API\n- WebSocket streaming\n- Request queuing and prioritization\n- A/B testing framework\n- Cost allocation by team/project\n- Prometheus/Grafana integration"
        }
      ]
    },
    {
      "week": 3,
      "title": "RAG Systems & Vector Databases",
      "description": "Build Retrieval-Augmented Generation systems to give LLMs access to your data. Learn vector databases, embeddings, and chunking strategies.",
      "lessons": [
        {
          "title": "Introduction to RAG Architecture",
          "content": "## What is RAG?\n\nRetrieval-Augmented Generation (RAG) combines the power of LLMs with your own data.\n\n### The Problem\n\nLLMs have limitations:\n- No knowledge beyond training cutoff\n- No access to private data\n- Can hallucinate facts\n- No source attribution\n\n### The Solution: RAG\n\n```\nUser Query → Retrieve Relevant Context → Combine with Query → LLM → Response\n```\n\n### RAG Architecture\n\n```\n┌─────────────────────────────────────────────────────────┐\n│                    RAG Pipeline                         │\n├─────────────────────────────────────────────────────────┤\n│                                                         │\n│  [Documents] → [Chunking] → [Embedding] → [Vector DB]  │\n│                                                         │\n│                                                       ↓ │\n│  [Query] → [Embedding] → [Similarity Search] → [Top K] │\n│                                                       ↓ │\n│           [Query + Context] → [LLM] → [Response]       │\n│                                                         │\n└─────────────────────────────────────────────────────────┘\n```\n\n### Key Components\n\n#### 1. Document Processing\n```python\nclass DocumentProcessor:\n    \"\"\"Process documents for RAG.\"\"\"\n    \n    def load(self, source: str) -> List[Document]:\n        \"\"\"Load documents from source.\"\"\"\n        pass\n    \n    def chunk(self, document: Document) -> List[Chunk]:\n        \"\"\"Split document into chunks.\"\"\"\n        pass\n    \n    def enrich(self, chunk: Chunk) -> Chunk:\n        \"\"\"Add metadata to chunks.\"\"\"\n        pass\n```\n\n#### 2. Embeddings\n```python\nclass EmbeddingModel:\n    \"\"\"Generate embeddings for text.\"\"\"\n    \n    def embed(self, text: str) -> List[float]:\n        \"\"\"Generate embedding vector.\"\"\"\n        pass\n    \n    def embed_batch(self, texts: List[str]) -> List[List[float]]:\n        \"\"\"Batch embedding for efficiency.\"\"\"\n        pass\n```\n\n#### 3. Vector Database\n```python\nclass VectorDB:\n    \"\"\"Store and search embeddings.\"\"\"\n    \n    def upsert(self, id: str, embedding: List[float], metadata: dict):\n        \"\"\"Store embedding with metadata.\"\"\"\n        pass\n    \n    def search(self, query_embedding: List[float], k: int) -> List[Result]:\n        \"\"\"Find k most similar vectors.\"\"\"\n        pass\n```\n\n#### 4. Retriever\n```python\nclass Retriever:\n    \"\"\"Retrieve relevant context.\"\"\"\n    \n    def retrieve(self, query: str, k: int = 5) -> List[Chunk]:\n        \"\"\"Get relevant chunks for query.\"\"\"\n        embedding = self.embed(query)\n        results = self.db.search(embedding, k)\n        return [r.chunk for r in results]\n```\n\n#### 5. Generator\n```python\nclass Generator:\n    \"\"\"Generate response with context.\"\"\"\n    \n    def generate(self, query: str, context: List[Chunk]) -> str:\n        \"\"\"Generate response using context.\"\"\"\n        prompt = self.build_prompt(query, context)\n        return self.llm.generate(prompt)\n```\n\n### Basic RAG Implementation\n\n```python\nclass SimpleRAG:\n    def __init__(self, embedding_model, vector_db, llm):\n        self.embedder = embedding_model\n        self.db = vector_db\n        self.llm = llm\n    \n    def index_documents(self, documents: List[str]):\n        \"\"\"Index documents for retrieval.\"\"\"\n        for i, doc in enumerate(documents):\n            # Chunk the document\n            chunks = self.chunk(doc)\n            \n            # Embed and store each chunk\n            for j, chunk in enumerate(chunks):\n                embedding = self.embedder.embed(chunk)\n                self.db.upsert(\n                    id=f\"doc_{i}_chunk_{j}\",\n                    embedding=embedding,\n                    metadata={\"text\": chunk, \"doc_id\": i}\n                )\n    \n    def query(self, question: str, k: int = 5) -> str:\n        \"\"\"Query the RAG system.\"\"\"\n        # Embed the query\n        query_embedding = self.embedder.embed(question)\n        \n        # Retrieve relevant chunks\n        results = self.db.search(query_embedding, k)\n        context = [r.metadata['text'] for r in results]\n        \n        # Generate response\n        prompt = f\"\"\"Answer the question based on the context.\n\nContext:\n{chr(10).join(context)}\n\nQuestion: {question}\n\nAnswer:\"\"\"\n        \n        return self.llm.generate(prompt)\n    \n    def chunk(self, text: str, size: int = 500, overlap: int = 50) -> List[str]:\n        \"\"\"Split text into overlapping chunks.\"\"\"\n        words = text.split()\n        chunks = []\n        \n        for i in range(0, len(words), size - overlap):\n            chunk = ' '.join(words[i:i + size])\n            chunks.append(chunk)\n        \n        return chunks\n```\n\n### RAG vs Fine-tuning\n\n| Aspect | RAG | Fine-tuning |\n|--------|-----|-------------|\n| Knowledge updates | Real-time | Requires retraining |\n| Cost | Lower | Higher |\n| Data privacy | Keep data private | Data in model |\n| Hallucinations | Reduced | Can still occur |\n| Source attribution | Yes | No |\n| Best for | Dynamic data, Q&A | Style, format, domain |\n\n### When to Use RAG\n\n**Good for:**\n- Question answering on your data\n- Document search and summarization\n- Customer support with knowledge base\n- Legal/medical document analysis\n- Code documentation search\n\n**Consider alternatives when:**\n- Need consistent style/format (fine-tuning)\n- Simple classification (few-shot)\n- Real-time data (function calling)\n\n### Hands-on Exercise\n\n1. Build a simple RAG system with:\n   - PDF document loader\n   - Basic chunking\n   - OpenAI embeddings\n   - In-memory vector store\n2. Test with a set of documents and queries\n3. Measure retrieval accuracy"
        },
        {
          "title": "Vector Databases Deep Dive",
          "content": "## Understanding Vector Databases\n\n### What are Vector Databases?\n\nVector databases store and search high-dimensional vectors efficiently.\n\n### How Vector Search Works\n\n```\n1. Convert text to vector (embedding)\n2. Store vectors with metadata\n3. Query: convert query to vector\n4. Find nearest vectors (similarity search)\n5. Return associated metadata\n```\n\n### Similarity Metrics\n\n```python\n# Cosine Similarity (most common)\ndef cosine_similarity(a: List[float], b: List[float]) -> float:\n    dot_product = sum(x * y for x, y in zip(a, b))\n    norm_a = sum(x ** 2 for x in a) ** 0.5\n    norm_b = sum(x ** 2 for x in b) ** 0.5\n    return dot_product / (norm_a * norm_b)\n\n# Euclidean Distance\ndef euclidean_distance(a: List[float], b: List[float]) -> float:\n    return sum((x - y) ** 2 for x, y in zip(a, b)) ** 0.5\n\n# Dot Product\ndef dot_product(a: List[float], b: List[float]) -> float:\n    return sum(x * y for x, y in zip(a, b))\n```\n\n### Vector Database Options\n\n#### 1. Pinecone (Managed)\n```python\nimport pinecone\n\npinecone.init(api_key=\"your-key\", environment=\"us-west1-gcp\")\nindex = pinecone.Index(\"my-index\")\n\n# Upsert\nindex.upsert([\n    (\"id1\", [0.1, 0.2, ...], {\"text\": \"document text\", \"source\": \"pdf\"}),\n    (\"id2\", [0.3, 0.4, ...], {\"text\": \"another text\", \"source\": \"web\"})\n])\n\n# Query\nresults = index.query(\n    vector=[0.15, 0.25, ...],\n    top_k=10,\n    include_metadata=True,\n    filter={\"source\": {\"$eq\": \"pdf\"}}\n)\n```\n\n#### 2. Chroma (Local/Managed)\n```python\nimport chromadb\n\nclient = chromadb.Client()\ncollection = client.create_collection(\"documents\")\n\n# Add documents\ncollection.add(\n    documents=[\"doc1 text\", \"doc2 text\"],\n    metadatas=[{\"source\": \"pdf\"}, {\"source\": \"web\"}],\n    ids=[\"id1\", \"id2\"]\n)\n\n# Query\nresults = collection.query(\n    query_texts=[\"search query\"],\n    n_results=5\n)\n```\n\n#### 3. Weaviate (Open Source)\n```python\nimport weaviate\n\nclient = weaviate.Client(\"http://localhost:8080\")\n\n# Add\nclient.data_object.create(\n    class_name=\"Document\",\n    data_object={\"text\": \"document content\", \"source\": \"pdf\"},\n    vector=[0.1, 0.2, ...]\n)\n\n# Query\nresult = client.query.get(\"Document\", [\"text\", \"source\"]).with_near_vector({\n    \"vector\": [0.15, 0.25, ...],\n    \"certainty\": 0.7\n}).with_limit(5).do()\n```\n\n#### 4. FAISS (Facebook, Local)\n```python\nimport faiss\nimport numpy as np\n\n# Create index\ndimension = 768  # embedding size\nindex = faiss.IndexFlatL2(dimension)\n\n# Add vectors\nvectors = np.array([[0.1, 0.2, ...], [0.3, 0.4, ...]], dtype=np.float32)\nindex.add(vectors)\n\n# Search\nquery = np.array([[0.15, 0.25, ...]], dtype=np.float32)\ndistances, indices = index.search(query, k=5)\n```\n\n### Advanced Features\n\n#### Filtering\n```python\n# Pre-filtering (filter then search)\nresults = index.query(\n    vector=query_embedding,\n    filter={\n        \"category\": \"technology\",\n        \"date\": {\"$gte\": \"2024-01-01\"}\n    },\n    top_k=10\n)\n```\n\n#### Hybrid Search (Vector + Keyword)\n```python\ndef hybrid_search(query: str, alpha: float = 0.5):\n    \"\"\"\n    Combine vector and keyword search.\n    alpha: weight for vector search (0-1)\n    \"\"\"\n    # Vector search\n    vector_results = vector_search(query, k=20)\n    \n    # Keyword search (BM25)\n    keyword_results = keyword_search(query, k=20)\n    \n    # Combine scores\n    combined = combine_results(\n        vector_results,\n        keyword_results,\n        weights=[alpha, 1 - alpha]\n    )\n    \n    return combined[:10]\n```\n\n#### Metadata Storage\n```python\nclass Document:\n    id: str\n    text: str\n    embedding: List[float]\n    metadata: dict\n    \n# Store rich metadata\ndocument = Document(\n    id=\"doc_123\",\n    text=\"Full document text...\",\n    embedding=[...],\n    metadata={\n        \"title\": \"Document Title\",\n        \"source\": \"https://example.com\",\n        \"author\": \"John Doe\",\n        \"date\": \"2024-01-15\",\n        \"category\": \"technology\",\n        \"tags\": [\"AI\", \"ML\", \"RAG\"],\n        \"page_number\": 5,\n        \"chunk_index\": 3\n    }\n)\n```\n\n### Performance Optimization\n\n#### Indexing Strategies\n\n```python\n# IVF (Inverted File Index) - faster, approximate\nnlist = 100  # number of clusters\nquantizer = faiss.IndexFlatL2(dimension)\nindex = faiss.IndexIVFFlat(quantizer, dimension, nlist)\n\n# HNSW (Hierarchical Navigable Small World) - very fast\nindex = faiss.IndexHNSWFlat(dimension, M=32)\n```\n\n#### Batch Operations\n```python\n# Batch insert for efficiency\ndef batch_insert(documents: List[Document], batch_size: int = 100):\n    for i in range(0, len(documents), batch_size):\n        batch = documents[i:i + batch_size]\n        ids = [d.id for d in batch]\n        embeddings = [d.embedding for d in batch]\n        metadatas = [d.metadata for d in batch]\n        \n        index.upsert(zip(ids, embeddings, metadatas))\n```\n\n### Hands-on Exercise\n\n1. Set up a vector database (Pinecone or Chroma)\n2. Index a corpus of documents\n3. Implement basic CRUD operations\n4. Add metadata filtering\n5. Compare different similarity metrics"
        },
        {
          "title": "Chunking Strategies and Embeddings",
          "content": "## Chunking Strategies\n\n### Why Chunking Matters\n\nDocuments are too large for:\n- Context windows\n- Accurate retrieval\n- Efficient embedding\n\n### Chunking Methods\n\n#### 1. Fixed Size Chunking\n```python\ndef fixed_size_chunk(text: str, chunk_size: int = 500, overlap: int = 50) -> List[str]:\n    \"\"\"Split by number of characters/words.\"\"\"\n    words = text.split()\n    chunks = []\n    \n    for i in range(0, len(words), chunk_size - overlap):\n        chunk = ' '.join(words[i:i + chunk_size])\n        chunks.append(chunk)\n    \n    return chunks\n```\n\n#### 2. Sentence Chunking\n```python\nimport nltk\n\ndef sentence_chunk(text: str, sentences_per_chunk: int = 3) -> List[str]:\n    \"\"\"Split by sentences.\"\"\"\n    nltk.download('punkt')\n    sentences = nltk.sent_tokenize(text)\n    \n    chunks = []\n    for i in range(0, len(sentences), sentences_per_chunk):\n        chunk = ' '.join(sentences[i:i + sentences_per_chunk])\n        chunks.append(chunk)\n    \n    return chunks\n```\n\n#### 3. Paragraph Chunking\n```python\ndef paragraph_chunk(text: str) -> List[str]:\n    \"\"\"Split by paragraphs.\"\"\"\n    paragraphs = text.split('\\n\\n')\n    return [p.strip() for p in paragraphs if p.strip()]\n```\n\n#### 4. Semantic Chunking\n```python\ndef semantic_chunk(text: str, threshold: float = 0.8) -> List[str]:\n    \"\"\"Split based on semantic similarity.\"\"\"\n    sentences = nltk.sent_tokenize(text)\n    \n    chunks = []\n    current_chunk = [sentences[0]]\n    \n    for i in range(1, len(sentences)):\n        # Compare embedding similarity\n        sim = cosine_similarity(\n            embed(' '.join(current_chunk)),\n            embed(sentences[i])\n        )\n        \n        if sim > threshold:\n            current_chunk.append(sentences[i])\n        else:\n            chunks.append(' '.join(current_chunk))\n            current_chunk = [sentences[i]]\n    \n    if current_chunk:\n        chunks.append(' '.join(current_chunk))\n    \n    return chunks\n```\n\n#### 5. Recursive Chunking\n```python\ndef recursive_chunk(\n    text: str,\n    chunk_size: int = 500,\n    overlap: int = 50,\n    separators: List[str] = None\n) -> List[str]:\n    \"\"\"\n    Recursively split by different separators.\n    Tries larger separators first.\n    \"\"\"\n    if separators is None:\n        separators = ['\\n\\n', '\\n', '. ', ' ', '']\n    \n    if len(text) <= chunk_size:\n        return [text]\n    \n    for separator in separators:\n        if separator in text:\n            parts = text.split(separator)\n            chunks = []\n            current_chunk = \"\"\n            \n            for part in parts:\n                if len(current_chunk) + len(part) <= chunk_size:\n                    current_chunk += part + separator\n                else:\n                    if current_chunk:\n                        chunks.append(current_chunk.strip())\n                    current_chunk = part + separator\n            \n            if current_chunk:\n                chunks.append(current_chunk.strip())\n            \n            return chunks\n    \n    # If no separator works, split by size\n    return [text[i:i+chunk_size] for i in range(0, len(text), chunk_size - overlap)]\n```\n\n### Embedding Models\n\n#### OpenAI Embeddings\n```python\nfrom openai import OpenAI\n\nclient = OpenAI()\n\ndef embed_text(text: str) -> List[float]:\n    response = client.embeddings.create(\n        model=\"text-embedding-3-small\",  # or text-embedding-3-large\n        input=text\n    )\n    return response.data[0].embedding\n\ndef embed_batch(texts: List[str]) -> List[List[float]]:\n    response = client.embeddings.create(\n        model=\"text-embedding-3-small\",\n        input=texts\n    )\n    return [d.embedding for d in response.data]\n```\n\n#### Open Source Embeddings\n```python\nfrom sentence_transformers import SentenceTransformer\n\nmodel = SentenceTransformer('all-MiniLM-L6-v2')\n\ndef embed_text(text: str) -> List[float]:\n    return model.encode(text).tolist()\n\ndef embed_batch(texts: List[str]) -> List[List[float]]:\n    return model.encode(texts).tolist()\n```\n\n### Embedding Model Comparison\n\n| Model | Dimensions | Quality | Speed | Cost |\n|-------|-----------|---------|-------|------|\n| text-embedding-3-small | 1536 | Good | Fast | Low |\n| text-embedding-3-large | 3072 | Excellent | Medium | Medium |\n| all-MiniLM-L6-v2 | 384 | Good | Very Fast | Free |\n| bge-large-en | 1024 | Very Good | Fast | Free |\n| e5-large-v2 | 1024 | Very Good | Fast | Free |\n\n### Chunking Best Practices\n\n```python\nclass SmartChunker:\n    \"\"\"Intelligent chunking with metadata.\"\"\"\n    \n    def __init__(\n        self,\n        chunk_size: int = 500,\n        chunk_overlap: int = 50,\n        length_function=len\n    ):\n        self.chunk_size = chunk_size\n        self.chunk_overlap = chunk_overlap\n        self.length_function = length_function\n    \n    def chunk_document(self, document: Document) -> List[Chunk]:\n        \"\"\"Chunk with metadata preservation.\"\"\"\n        # Choose strategy based on document type\n        if document.type == 'code':\n            chunks = self.chunk_code(document.text)\n        elif document.type == 'markdown':\n            chunks = self.chunk_markdown(document.text)\n        else:\n            chunks = self.recursive_chunk(document.text)\n        \n        # Add metadata\n        return [\n            Chunk(\n                id=f\"{document.id}_chunk_{i}\",\n                text=chunk,\n                metadata={\n                    **document.metadata,\n                    'chunk_index': i,\n                    'total_chunks': len(chunks),\n                    'chunk_size': len(chunk)\n                }\n            )\n            for i, chunk in enumerate(chunks)\n        ]\n    \n    def chunk_code(self, code: str) -> List[str]:\n        \"\"\"Chunk code by functions/classes.\"\"\"\n        # Use AST parsing for Python\n        # Use regex for other languages\n        pass\n    \n    def chunk_markdown(self, markdown: str) -> List[str]:\n        \"\"\"Chunk markdown by headers.\"\"\"\n        # Split by headers first\n        # Then apply recursive chunking\n        pass\n```\n\n### Hands-on Exercise\n\n1. Implement 3 different chunking strategies\n2. Compare their performance on a test corpus\n3. Build a chunker that auto-selects strategy based on content type\n4. Measure retrieval quality for each strategy"
        },
        {
          "title": "Week 3 Project: Production RAG System",
          "content": "## Project: Build a Production RAG System\n\n### Objective\nCreate a complete RAG system that can answer questions about a document corpus with source attribution.\n\n### Requirements\n\n#### 1. Document Ingestion Pipeline\n```python\nclass IngestionPipeline:\n    \"\"\"\n    Ingest documents from multiple sources.\n    \"\"\"\n    \n    def ingest_file(self, file_path: str) -> Document:\n        \"\"\"Ingest a single file.\"\"\"\n        pass\n    \n    def ingest_directory(self, dir_path: str, recursive: bool = True):\n        \"\"\"Ingest all files in directory.\"\"\"\n        pass\n    \n    def ingest_url(self, url: str) -> Document:\n        \"\"\"Ingest from URL.\"\"\"\n        pass\n    \n    def supported_formats(self) -> List[str]:\n        \"\"\"Return supported file formats.\"\"\"\n        return ['pdf', 'txt', 'md', 'html', 'docx']\n```\n\n#### 2. Chunking and Embedding\n```python\nclass ChunkingPipeline:\n    \"\"\"\n    Chunk and embed documents.\n    \"\"\"\n    \n    def __init__(self, strategy: str = 'recursive', embedding_model: str = 'openai'):\n        pass\n    \n    def process(self, document: Document) -> List[Chunk]:\n        \"\"\"Chunk document and generate embeddings.\"\"\"\n        pass\n```\n\n#### 3. Vector Store\n```python\nclass VectorStore:\n    \"\"\"\n    Store and retrieve document chunks.\n    \"\"\"\n    \n    def add_chunks(self, chunks: List[Chunk]):\n        \"\"\"Add chunks to the store.\"\"\"\n        pass\n    \n    def search(self, query: str, k: int = 5, filters: dict = None) -> List[Result]:\n        \"\"\"Search for relevant chunks.\"\"\"\n        pass\n    \n    def delete(self, document_id: str):\n        \"\"\"Delete all chunks for a document.\"\"\"\n        pass\n```\n\n#### 4. Retrieval System\n```python\nclass Retriever:\n    \"\"\"\n    Retrieve relevant context for queries.\n    \"\"\"\n    \n    def retrieve(\n        self,\n        query: str,\n        k: int = 5,\n        rerank: bool = True,\n        filters: dict = None\n    ) -> List[Chunk]:\n        \"\"\"Retrieve relevant chunks.\"\"\"\n        pass\n    \n    def hybrid_search(\n        self,\n        query: str,\n        alpha: float = 0.5\n    ) -> List[Chunk]:\n        \"\"\"Combine vector and keyword search.\"\"\"\n        pass\n```\n\n#### 5. Response Generation\n```python\nclass RAGGenerator:\n    \"\"\"\n    Generate responses with retrieved context.\n    \"\"\"\n    \n    def generate(\n        self,\n        query: str,\n        context: List[Chunk],\n        max_tokens: int = 500\n    ) -> Response:\n        \"\"\"\n        Generate response with citations.\n        \"\"\"\n        pass\n    \n    def build_prompt(self, query: str, context: List[Chunk]) -> str:\n        \"\"\"Build prompt with context.\"\"\"\n        pass\n```\n\n### Project Structure\n\n```\nrag_system/\n├── ingestion/\n│   ├── __init__.py\n│   ├── loaders/\n│   │   ├── pdf_loader.py\n│   │   ├── web_loader.py\n│   │   └── text_loader.py\n│   └── pipeline.py\n├── chunking/\n│   ├── __init__.py\n│   ├── strategies.py\n│   └── smart_chunker.py\n├── embeddings/\n│   ├── __init__.py\n│   ├── openai_embeddings.py\n│   └── local_embeddings.py\n├── storage/\n│   ├── __init__.py\n│   ├── vector_store.py\n│   └── metadata_store.py\n├── retrieval/\n│   ├── __init__.py\n│   ├── retriever.py\n│   └── reranker.py\n├── generation/\n│   ├── __init__.py\n│   ├── prompt_builder.py\n│   └── generator.py\n├── api/\n│   ├── __init__.py\n│   ├── routes.py\n│   └── schemas.py\n└── cli.py\n```\n\n### Features to Implement\n\n1. **Multi-format Support**: PDF, TXT, MD, HTML, DOCX\n2. **Smart Chunking**: Auto-detect optimal chunk size\n3. **Hybrid Search**: Vector + keyword search\n4. **Reranking**: Improve retrieval quality\n5. **Citations**: Link responses to sources\n6. **Streaming**: Stream responses\n7. **Caching**: Cache common queries\n8. **Analytics**: Track query performance\n\n### API Endpoints\n\n```python\n# Upload and index documents\nPOST /documents/upload\nContent-Type: multipart/form-data\nfile: document.pdf\n\n# Ingest from URL\nPOST /documents/url\n{\n  \"url\": \"https://example.com/article\"\n}\n\n# Query the RAG system\nPOST /query\n{\n  \"query\": \"What is the main topic?\",\n  \"k\": 5,\n  \"filters\": {\"source\": \"pdf\"},\n  \"stream\": false\n}\n\n# Get document info\nGET /documents/{id}\n\n# Delete document\nDELETE /documents/{id}\n```\n\n### Response Format\n\n```json\n{\n  \"query\": \"What is RAG?\",\n  \"answer\": \"RAG stands for Retrieval-Augmented Generation...\",\n  \"sources\": [\n    {\n      \"document_id\": \"doc_123\",\n      \"chunk_id\": \"chunk_5\",\n      \"text\": \"RAG combines retrieval with generation...\",\n      \"score\": 0.92,\n      \"metadata\": {\n        \"title\": \"Introduction to RAG\",\n        \"page\": 5\n      }\n    }\n  ],\n  \"confidence\": 0.85,\n  \"latency_ms\": 450\n}\n```\n\n### Deliverables\n\n1. **Working RAG system** with all features\n2. **CLI tool**:\n   ```bash\n   rag ingest ./documents --recursive\n   rag query \"What is machine learning?\"\n   rag list-documents\n   rag delete doc_123\n   ```\n3. **Docker setup** for deployment\n4. **Tests** with sample corpus\n5. **Documentation** and usage examples\n\n### Evaluation Criteria\n\n- Retrieval accuracy (25%)\n- Response quality (20%)\n- Code quality (20%)\n- Feature completeness (15%)\n- Performance (10%)\n- Documentation (10%)\n\n### Extension Ideas\n\n- Multi-modal RAG (images, tables)\n- Conversational memory\n- Query reformulation\n- Document versioning\n- Access control\n- Real-time index updates"
        }
      ]
    },
    {
      "week": 4,
      "title": "AI Agent Architecture",
      "description": "Learn to build AI agents that can reason, plan, and take actions. Understand agent patterns, memory systems, and autonomous execution.",
      "lessons": [
        {
          "title": "What Are AI Agents?",
          "content": "## Understanding AI Agents\n\n### Definition\n\nAn AI agent is a system that:\n1. **Perceives** its environment\n2. **Reasons** about what to do\n3. **Acts** to achieve goals\n4. **Learns** from results\n\n### Agents vs. Simple LLM Calls\n\n```\nSimple LLM:\nUser → LLM → Response\n\nAI Agent:\nUser → Agent → [Think → Act → Observe] × N → Response\n```\n\n### Agent Components\n\n```\n┌─────────────────────────────────────┐\n│           AI Agent                  │\n├─────────────────────────────────────┤\n│                                     │\n│  ┌─────────────────────────────┐   │\n│  │        Brain (LLM)          │   │\n│  │   - Reasoning               │   │\n│  │   - Planning                │   │\n│  │   - Decision making         │   │\n│  └─────────────────────────────┘   │\n│              ↕                      │\n│  ┌─────────────────────────────┐   │\n│  │         Memory              │   │\n│  │   - Short-term (context)    │   │\n│  │   - Long-term (vector DB)   │   │\n│  │   - Working (scratchpad)    │   │\n│  └─────────────────────────────┘   │\n│              ↕                      │\n│  ┌─────────────────────────────┐   │\n│  │          Tools              │   │\n│  │   - Web search              │   │\n│  │   - Calculator              │   │\n│  │   - Code execution          │   │\n│  │   - API calls               │   │\n│  └─────────────────────────────┘   │\n│                                     │\n└─────────────────────────────────────┘\n```\n\n### Agent Loop (ReAct Pattern)\n\n```python\nclass Agent:\n    def run(self, task: str) -> str:\n        while not self.is_complete():\n            # Think: Analyze current state\n            thought = self.think(task)\n            \n            # Act: Choose and execute action\n            action = self.decide_action(thought)\n            result = self.execute(action)\n            \n            # Observe: Learn from result\n            self.observe(result)\n        \n        return self.final_answer()\n```\n\n### Types of Agents\n\n#### 1. Reactive Agents\n- Respond to immediate inputs\n- No memory or planning\n- Example: Simple chatbot\n\n#### 2. Deliberative Agents\n- Plan before acting\n- Maintain internal state\n- Example: Chess-playing AI\n\n#### 3. Hybrid Agents\n- Combine reactive and deliberative\n- Fast responses + strategic planning\n- Example: Virtual assistant\n\n### Agent Architectures\n\n#### Simple Agent\n```python\nclass SimpleAgent:\n    def __init__(self, llm, tools):\n        self.llm = llm\n        self.tools = tools\n    \n    def run(self, query: str) -> str:\n        prompt = f\"\"\"\n        You have access to these tools: {self.tools}\n        \n        Query: {query}\n        \n        Think about what to do, then respond.\n        \"\"\"\n        return self.llm.generate(prompt)\n```\n\n#### ReAct Agent\n```python\nclass ReActAgent:\n    def __init__(self, llm, tools):\n        self.llm = llm\n        self.tools = tools\n        self.memory = []\n    \n    def run(self, query: str, max_iterations: int = 5):\n        for i in range(max_iterations):\n            # Generate thought and action\n            response = self.llm.generate(\n                self.build_prompt(query),\n                stop=[\"Observation:\"]\n            )\n            \n            # Parse action\n            thought, action = self.parse(response)\n            \n            # Execute and observe\n            if action:\n                observation = self.execute(action)\n                self.memory.append({\n                    'thought': thought,\n                    'action': action,\n                    'observation': observation\n                })\n            \n            # Check if done\n            if \"Answer:\" in response:\n                return self.extract_answer(response)\n        \n        return \"Could not complete task\"\n```\n\n### Agent Capabilities\n\n**Reasoning**\n- Break down complex tasks\n- Evaluate options\n- Make decisions\n\n**Planning**\n- Create multi-step plans\n- Adjust plans based on results\n- Handle failures\n\n**Memory**\n- Remember conversation history\n- Store learned facts\n- Retrieve relevant information\n\n**Tool Use**\n- Search the web\n- Execute code\n- Call APIs\n- Read/write files\n\n### Real-World Agent Examples\n\n1. **Research Agent**\n   - Search for information\n   - Synthesize findings\n   - Generate reports\n\n2. **Coding Agent**\n   - Understand requirements\n   - Write code\n   - Test and debug\n\n3. **Customer Service Agent**\n   - Understand issues\n   - Access knowledge base\n   - Resolve problems\n\n### Hands-on Exercise\n\n1. Build a simple agent that can:\n   - Answer questions\n   - Use a calculator tool\n   - Search mock database\n2. Implement the ReAct loop\n3. Test with multi-step tasks"
        },
        {
          "title": "Agent Memory Systems",
          "content": "## Agent Memory Architecture\n\n### Types of Memory\n\n#### 1. Short-Term Memory (Context)\n```python\nclass ShortTermMemory:\n    \"\"\"\n    Temporary storage in context window.\n    \"\"\"\n    \n    def __init__(self, max_tokens: int = 4000):\n        self.messages = []\n        self.max_tokens = max_tokens\n    \n    def add(self, role: str, content: str):\n        self.messages.append({\"role\": role, \"content\": content})\n        self.trim_if_needed()\n    \n    def get_context(self) -> List[dict]:\n        return self.messages\n    \n    def trim_if_needed(self):\n        while self.count_tokens() > self.max_tokens:\n            # Remove oldest non-system message\n            for i, msg in enumerate(self.messages):\n                if msg['role'] != 'system':\n                    self.messages.pop(i)\n                    break\n```\n\n#### 2. Long-Term Memory (Vector Store)\n```python\nclass LongTermMemory:\n    \"\"\"\n    Persistent storage using vector embeddings.\n    \"\"\"\n    \n    def __init__(self, embedding_model, vector_db):\n        self.embedder = embedding_model\n        self.db = vector_db\n    \n    def store(self, content: str, metadata: dict = None):\n        \"\"\"Store a memory.\"\"\"\n        embedding = self.embedder.embed(content)\n        memory_id = str(uuid.uuid4())\n        \n        self.db.upsert(\n            id=memory_id,\n            embedding=embedding,\n            metadata={\n                \"content\": content,\n                \"timestamp\": datetime.now().isoformat(),\n                **(metadata or {})\n            }\n        )\n    \n    def recall(self, query: str, k: int = 5) -> List[dict]:\n        \"\"\"Retrieve relevant memories.\"\"\"\n        query_embedding = self.embedder.embed(query)\n        results = self.db.search(query_embedding, k)\n        \n        return [\n            {\"content\": r.metadata['content'], \"score\": r.score}\n            for r in results\n        ]\n```\n\n#### 3. Working Memory (Scratchpad)\n```python\nclass WorkingMemory:\n    \"\"\"\n    Temporary scratchpad for complex tasks.\n    \"\"\"\n    \n    def __init__(self):\n        self.data = {}\n        self.history = []\n    \n    def set(self, key: str, value: Any):\n        self.data[key] = value\n        self.history.append({\n            'action': 'set',\n            'key': key,\n            'value': value,\n            'timestamp': datetime.now()\n        })\n    \n    def get(self, key: str) -> Any:\n        return self.data.get(key)\n    \n    def to_context(self) -> str:\n        \"\"\"Format for inclusion in prompts.\"\"\"\n        lines = [\"Current state:\"]\n        for key, value in self.data.items():\n            lines.append(f\"  {key}: {value}\")\n        return '\\n'.join(lines)\n```\n\n### Memory Management Strategies\n\n#### Sliding Window\n```python\nclass SlidingWindowMemory:\n    \"\"\"\n    Keep last N interactions.\n    \"\"\"\n    \n    def __init__(self, window_size: int = 10):\n        self.window_size = window_size\n        self.messages = []\n    \n    def add(self, message: dict):\n        self.messages.append(message)\n        if len(self.messages) > self.window_size:\n            self.messages.pop(0)\n```\n\n#### Summary-Based\n```python\nclass SummaryMemory:\n    \"\"\"\n    Summarize old conversations.\n    \"\"\"\n    \n    def __init__(self, llm, max_before_summary: int = 10):\n        self.llm = llm\n        self.max_before_summary = max_before_summary\n        self.recent = []\n        self.summary = \"\"\n    \n    def add(self, message: dict):\n        self.recent.append(message)\n        \n        if len(self.recent) > self.max_before_summary:\n            self.summarize_old()\n    \n    def summarize_old(self):\n        to_summarize = self.recent[:-self.max_before_summary//2]\n        self.recent = self.recent[-self.max_before_summary//2:]\n        \n        prompt = f\"Summarize this conversation:\\n{to_summarize}\"\n        new_summary = self.llm.generate(prompt)\n        \n        self.summary = f\"Previous context: {new_summary}\\n\\n{self.summary}\"\n```\n\n#### Entity-Based\n```python\nclass EntityMemory:\n    \"\"\"\n    Track entities and their attributes.\n    \"\"\"\n    \n    def __init__(self):\n        self.entities = {}  # {entity_name: {attribute: value}}\n    \n    def update(self, entity: str, attributes: dict):\n        if entity not in self.entities:\n            self.entities[entity] = {}\n        self.entities[entity].update(attributes)\n    \n    def get(self, entity: str) -> dict:\n        return self.entities.get(entity, {})\n    \n    def to_context(self) -> str:\n        lines = [\"Known entities:\"]\n        for entity, attrs in self.entities.items():\n            attrs_str = ', '.join(f\"{k}: {v}\" for k, v in attrs.items())\n            lines.append(f\"  {entity}: {attrs_str}\")\n        return '\\n'.join(lines)\n```\n\n### Complete Memory System\n\n```python\nclass AgentMemory:\n    \"\"\"\n    Unified memory system for agents.\n    \"\"\"\n    \n    def __init__(self, llm, embedding_model, vector_db):\n        self.short_term = ShortTermMemory(max_tokens=4000)\n        self.long_term = LongTermMemory(embedding_model, vector_db)\n        self.working = WorkingMemory()\n        self.summary = SummaryMemory(llm)\n    \n    def remember(self, content: str, importance: float = 0.5):\n        \"\"\"Store information in appropriate memory.\"\"\"\n        if importance > 0.7:\n            # Important → long-term\n            self.long_term.store(content)\n        \n        # Always add to short-term\n        self.short_term.add(\"assistant\", content)\n    \n    def recall(self, query: str) -> str:\n        \"\"\"Retrieve relevant memories.\"\"\"\n        # Get from long-term\n        long_term = self.long_term.recall(query, k=3)\n        \n        # Get summary\n        summary = self.summary.summary\n        \n        # Get working state\n        working = self.working.to_context()\n        \n        # Combine\n        context_parts = []\n        if summary:\n            context_parts.append(f\"Context: {summary}\")\n        if long_term:\n            context_parts.append(\"Relevant memories:\")\n            for mem in long_term:\n                context_parts.append(f\"  - {mem['content']}\")\n        if working:\n            context_parts.append(working)\n        \n        return '\\n'.join(context_parts)\n```\n\n### Memory in Agent Loop\n\n```python\nclass AgentWithMemory:\n    def __init__(self, llm, memory: AgentMemory):\n        self.llm = llm\n        self.memory = memory\n    \n    def run(self, query: str):\n        # Retrieve relevant memories\n        context = self.memory.recall(query)\n        \n        # Build prompt with memory\n        prompt = f\"\"\"\n{context}\n\nUser: {query}\n\nThink about what to do.\n\"\"\"\n        \n        # Get response\n        response = self.llm.generate(prompt)\n        \n        # Store important parts\n        self.memory.remember(response, importance=0.5)\n        \n        return response\n```\n\n### Hands-on Exercise\n\n1. Implement all three memory types\n2. Build a memory manager that auto-selects storage\n3. Test memory persistence across sessions\n4. Implement memory importance scoring"
        },
        {
          "title": "Planning and Execution",
          "content": "## Agent Planning\n\n### Why Planning Matters\n\nComplex tasks require:\n- Breaking into steps\n- Ordering operations\n- Handling dependencies\n- Adapting to failures\n\n### Planning Approaches\n\n#### 1. Single-Step Planning\n```python\ndef plan_single_step(task: str) -> List[str]:\n    \"\"\"Generate plan once at the start.\"\"\"\n    prompt = f\"\"\"\n    Task: {task}\n    \n    Create a step-by-step plan to complete this task.\n    Format as a numbered list.\n    \"\"\"\n    \n    response = llm.generate(prompt)\n    return parse_steps(response)\n```\n\n#### 2. Multi-Step Planning (Plan-and-Execute)\n```python\nclass PlanAndExecute:\n    def __init__(self, llm, tools):\n        self.llm = llm\n        self.tools = tools\n    \n    def run(self, task: str):\n        # Planning phase\n        plan = self.create_plan(task)\n        \n        # Execution phase\n        results = []\n        for step in plan:\n            result = self.execute_step(step)\n            results.append(result)\n            \n            # Replan if needed\n            if not self.is_success(result):\n                plan = self.replan(task, plan, results)\n        \n        return self.synthesize(results)\n    \n    def create_plan(self, task: str) -> List[Step]:\n        prompt = f\"\"\"\n        Task: {task}\n        \n        Create a detailed plan. For each step specify:\n        1. Description\n        2. Tool to use (if any)\n        3. Expected output\n        \"\"\"\n        response = self.llm.generate(prompt)\n        return self.parse_plan(response)\n```\n\n#### 3. Dynamic Replanning\n```python\nclass AdaptivePlanner:\n    def __init__(self, llm, max_replans: int = 3):\n        self.llm = llm\n        self.max_replans = max_replans\n    \n    def execute_with_replanning(self, task: str):\n        plan = self.create_plan(task)\n        completed = []\n        replans = 0\n        \n        while plan and replans < self.max_replans:\n            step = plan[0]\n            result = self.execute(step)\n            \n            if self.is_success(result):\n                completed.append((step, result))\n                plan.pop(0)\n            else:\n                # Replan from failure point\n                plan = self.replan_from(\n                    task,\n                    completed,\n                    step,\n                    result\n                )\n                replans += 1\n        \n        return completed\n```\n\n### Task Decomposition\n\n```python\nclass TaskDecomposer:\n    \"\"\"Break complex tasks into subtasks.\"\"\"\n    \n    def decompose(self, task: str) -> List[SubTask]:\n        prompt = f\"\"\"\n        Task: {task}\n        \n        Break this into subtasks. Each subtask should be:\n        1. Atomic (can't be split further)\n        2. Clear success criteria\n        3. Independent where possible\n        \n        Output format:\n        - Subtask 1: [description]\n          Success: [criteria]\n          Depends on: [none|subtask ids]\n        \"\"\"\n        \n        response = self.llm.generate(prompt)\n        return self.parse_subtasks(response)\n    \n    def build_dependency_graph(self, subtasks: List[SubTask]):\n        \"\"\"Build execution graph based on dependencies.\"\"\"\n        graph = {}\n        for task in subtasks:\n            graph[task.id] = {\n                'task': task,\n                'dependencies': task.depends_on,\n                'status': 'pending'\n            }\n        return graph\n```\n\n### Execution Strategies\n\n#### Sequential\n```python\ndef execute_sequential(plan: List[Step]):\n    results = []\n    for step in plan:\n        result = execute(step)\n        results.append(result)\n    return results\n```\n\n#### Parallel (for independent tasks)\n```python\nimport asyncio\n\nasync def execute_parallel(tasks: List[Task]):\n    \"\"\"Execute independent tasks in parallel.\"\"\"\n    results = await asyncio.gather(*[\n        execute_async(task) for task in tasks\n    ])\n    return results\n```\n\n#### Conditional\n```python\ndef execute_conditional(plan: List[ConditionalStep]):\n    results = []\n    for step in plan:\n        if evaluate_condition(step.condition, results):\n            result = execute(step)\n            results.append(result)\n    return results\n```\n\n### Self-Reflection and Improvement\n\n```python\nclass SelfReflectingAgent:\n    def __init__(self, llm):\n        self.llm = llm\n    \n    def execute_with_reflection(self, task: str):\n        # Initial attempt\n        result = self.execute(task)\n        \n        # Reflect on result\n        reflection = self.reflect(task, result)\n        \n        # Improve if needed\n        if reflection['needs_improvement']:\n            improved = self.improve(\n                task,\n                result,\n                reflection['suggestions']\n            )\n            return improved\n        \n        return result\n    \n    def reflect(self, task: str, result: str) -> dict:\n        prompt = f\"\"\"\n        Task: {task}\n        Result: {result}\n        \n        Evaluate the result:\n        1. Was the task completed successfully?\n        2. What could be improved?\n        3. Were there any errors or issues?\n        \n        Output JSON:\n        {{\n          \"success\": true/false,\n          \"needs_improvement\": true/false,\n          \"suggestions\": [\"...\"],\n          \"issues\": [\"...\"]\n        }}\n        \"\"\"\n        \n        response = self.llm.generate(prompt)\n        return json.loads(response)\n```\n\n### Goal Decomposition Tree\n\n```python\nclass GoalTree:\n    \"\"\"\n    Hierarchical goal decomposition.\n    \"\"\"\n    \n    def __init__(self, root_goal: str):\n        self.root = GoalNode(root_goal)\n    \n    def decompose(self, depth: int = 2):\n        \"\"\"Recursively decompose goals.\"\"\"\n        self._decompose_node(self.root, depth)\n    \n    def _decompose_node(self, node: GoalNode, remaining_depth: int):\n        if remaining_depth <= 0:\n            return\n        \n        subgoals = self.get_subgoals(node.goal)\n        for sg in subgoals:\n            child = GoalNode(sg)\n            node.add_child(child)\n            self._decompose_node(child, remaining_depth - 1)\n    \n    def to_plan(self) -> List[Step]:\n        \"\"\"Convert tree to linear plan.\"\"\"\n        return self.flatten(self.root)\n```\n\n### Hands-on Exercise\n\n1. Build a plan-and-execute agent\n2. Implement dynamic replanning\n3. Create a task dependency resolver\n4. Add self-reflection loop"
        },
        {
          "title": "Week 4 Project: Autonomous Agent",
          "content": "## Project: Build an Autonomous Research Agent\n\n### Objective\nCreate an agent that can research a topic autonomously, gathering information, synthesizing findings, and producing a report.\n\n### Requirements\n\n#### 1. Agent Core\n```python\nclass ResearchAgent:\n    \"\"\"\n    Autonomous research agent.\n    \"\"\"\n    \n    def __init__(self, llm, memory, tools):\n        self.llm = llm\n        self.memory = memory\n        self.tools = tools\n        self.planner = Planner(llm)\n        self.executor = Executor(tools)\n    \n    def research(self, topic: str, depth: int = 3) -> Report:\n        \"\"\"Conduct research on a topic.\"\"\"\n        pass\n```\n\n#### 2. Planning System\n```python\nclass ResearchPlanner:\n    \"\"\"\n    Create research plans.\n    \"\"\"\n    \n    def create_plan(self, topic: str, depth: int) -> ResearchPlan:\n        \"\"\"\n        Generate research plan with:\n        - Key questions to answer\n        - Sources to check\n        - Subtopics to explore\n        \"\"\"\n        pass\n    \n    def adjust_plan(self, current_plan: ResearchPlan, findings: List[Finding]) -> ResearchPlan:\n        \"\"\"Adjust plan based on discoveries.\"\"\"\n        pass\n```\n\n#### 3. Tool Integration\n```python\nclass ResearchTools:\n    \"\"\"\n    Tools for research.\n    \"\"\"\n    \n    @tool\n    def web_search(self, query: str) -> List[SearchResult]:\n        \"\"\"Search the web.\"\"\"\n        pass\n    \n    @tool\n    def scrape_page(self, url: str) -> str:\n        \"\"\"Extract text from webpage.\"\"\"\n        pass\n    \n    @tool\n    def extract_facts(self, text: str) -> List[Fact]:\n        \"\"\"Extract key facts from text.\"\"\"\n        pass\n    \n    @tool\n    def compare_sources(self, sources: List[str]) -> Comparison:\n        \"\"\"Compare information across sources.\"\"\"\n        pass\n```\n\n#### 4. Memory System\n```python\nclass ResearchMemory:\n    \"\"\"\n    Track research progress.\n    \"\"\"\n    \n    def __init__(self):\n        self.findings = []  # Discovered facts\n        self.sources = []   # Visited sources\n        self.questions = [] # Questions explored\n        self.gaps = []      # Knowledge gaps\n    \n    def add_finding(self, finding: Finding):\n        pass\n    \n    def mark_explored(self, question: str):\n        pass\n    \n    def identify_gaps(self) -> List[str]:\n        \"\"\"Find unanswered questions.\"\"\"\n        pass\n```\n\n#### 5. Report Generator\n```python\nclass ReportGenerator:\n    \"\"\"\n    Generate research reports.\n    \"\"\"\n    \n    def generate(self, topic: str, findings: List[Finding]) -> Report:\n        \"\"\"\n        Create comprehensive report:\n        - Executive summary\n        - Key findings\n        - Detailed analysis\n        - Sources\n        - Confidence levels\n        \"\"\"\n        pass\n```\n\n### Project Structure\n\n```\nresearch_agent/\n├── core/\n│   ├── __init__.py\n│   ├── agent.py\n│   └── types.py\n├── planning/\n│   ├── __init__.py\n│   ├── planner.py\n│   └── plan_types.py\n├── tools/\n│   ├── __init__.py\n│   ├── web_search.py\n│   ├── scraper.py\n│   ├── fact_extractor.py\n│   └── source_compare.py\n├── memory/\n│   ├── __init__.py\n│   ├── research_memory.py\n│   └── fact_store.py\n├── generation/\n│   ├── __init__.py\n│   ├── report_generator.py\n│   └── templates.py\n├── evaluation/\n│   ├── __init__.py\n│   ├── credibility.py\n│   └── completeness.py\n└── cli.py\n```\n\n### Agent Workflow\n\n```\n1. Receive topic\n   ↓\n2. Create research plan\n   - Key questions\n   - Subtopics\n   - Source types\n   ↓\n3. Execute plan (loop)\n   - Search for information\n   - Scrape relevant pages\n   - Extract facts\n   - Store in memory\n   - Identify gaps\n   - Replan if needed\n   ↓\n4. Synthesize findings\n   - Group by topic\n   - Identify consensus/conflicts\n   - Assess credibility\n   ↓\n5. Generate report\n   - Executive summary\n   - Key findings\n   - Supporting evidence\n   - Source citations\n   - Confidence scores\n```\n\n### Example Usage\n\n```bash\nresearch-agent research \"The impact of AI on healthcare\" --depth 3 --output report.md\n\n# Output:\n# Researching: The impact of AI on healthcare\n# Plan created: 5 questions, 12 subtopics\n# Searching... Found 15 relevant sources\n# Analyzing... Extracted 47 facts\n# Synthesizing... Identified 3 main themes\n# Report generated: report.md\n```\n\n### Report Format\n\n```markdown\n# Research Report: The Impact of AI on Healthcare\n\n## Executive Summary\n[2-3 paragraph overview]\n\n## Key Findings\n\n### 1. Diagnostic Accuracy\n- AI systems achieve X% accuracy in Y condition\n- [Source 1, Source 2]\n- Confidence: High\n\n### 2. Administrative Efficiency\n...\n\n## Detailed Analysis\n[In-depth analysis of each theme]\n\n## Open Questions\n[Areas requiring further research]\n\n## Sources\n1. Source Title - URL - Credibility: High\n2. ...\n\n## Methodology\n[How research was conducted]\n```\n\n### Deliverables\n\n1. **Working research agent** with all components\n2. **CLI tool** for easy use\n3. **Sample research reports** on 3 topics\n4. **Tests** with mock sources\n5. **Documentation** and examples\n\n### Evaluation Criteria\n\n- Research quality (30%)\n- Autonomy and planning (20%)\n- Code quality (20%)\n- Report quality (15%)\n- Tool integration (15%)\n\n### Extension Ideas\n\n- Multi-agent collaboration\n- Real-time source monitoring\n- Fact verification with cross-references\n- Bias detection\n- Visual report generation\n- Export to various formats"
        }
      ]
    },
    {
      "week": 5,
      "title": "Function Calling & Tools",
      "description": "Enable agents to interact with the world through function calling. Build tools, handle parameters, and manage complex tool workflows.",
      "lessons": [
        {
          "title": "Understanding Function Calling",
          "content": "## What is Function Calling?\n\nFunction calling allows LLMs to:\n1. **Request execution** of predefined functions\n2. **Pass parameters** extracted from context\n3. **Receive results** to inform next actions\n\n### How It Works\n\n```\nUser Request\n    ↓\nLLM analyzes request\n    ↓\nLLM decides to call function\n    ↓\nLLM outputs function call (name + params)\n    ↓\nYour code executes function\n    ↓\nResult sent back to LLM\n    ↓\nLLM generates final response\n```\n\n### OpenAI Function Calling\n\n#### Define Functions\n```python\nfunctions = [\n    {\n        \"name\": \"get_weather\",\n        \"description\": \"Get current weather for a location\",\n        \"parameters\": {\n            \"type\": \"object\",\n            \"properties\": {\n                \"location\": {\n                    \"type\": \"string\",\n                    \"description\": \"City and country, e.g., 'Tokyo, Japan'\"\n                },\n                \"unit\": {\n                    \"type\": \"string\",\n                    \"enum\": [\"celsius\", \"fahrenheit\"],\n                    \"description\": \"Temperature unit\"\n                }\n            },\n            \"required\": [\"location\"]\n        }\n    },\n    {\n        \"name\": \"search_web\",\n        \"description\": \"Search the web for information\",\n        \"parameters\": {\n            \"type\": \"object\",\n            \"properties\": {\n                \"query\": {\n                    \"type\": \"string\",\n                    \"description\": \"Search query\"\n                },\n                \"num_results\": {\n                    \"type\": \"integer\",\n                    \"description\": \"Number of results\",\n                    \"default\": 5\n                }\n            },\n            \"required\": [\"query\"]\n        }\n    }\n]\n```\n\n#### Call API with Functions\n```python\nresponse = client.chat.completions.create(\n    model=\"gpt-4-turbo-preview\",\n    messages=[\n        {\"role\": \"user\", \"content\": \"What's the weather in Tokyo?\"}\n    ],\n    functions=functions,\n    function_call=\"auto\"\n)\n\nmessage = response.choices[0].message\n\n# Check if function was called\nif message.function_call:\n    function_name = message.function_call.name\n    arguments = json.loads(message.function_call.arguments)\n    \n    print(f\"Function: {function_name}\")\n    print(f\"Arguments: {arguments}\")\n    # Output:\n    # Function: get_weather\n    # Arguments: {\"location\": \"Tokyo, Japan\"}\n```\n\n#### Execute and Return Result\n```python\n# Execute the function\nif function_name == \"get_weather\":\n    result = get_weather(\n        location=arguments[\"location\"],\n        unit=arguments.get(\"unit\", \"celsius\")\n    )\n\n# Send result back to LLM\nsecond_response = client.chat.completions.create(\n    model=\"gpt-4-turbo-preview\",\n    messages=[\n        {\"role\": \"user\", \"content\": \"What's the weather in Tokyo?\"},\n        message,  # First response with function call\n        {\n            \"role\": \"function\",\n            \"name\": function_name,\n            \"content\": json.dumps(result)\n        }\n    ]\n)\n\nfinal_answer = second_response.choices[0].message.content\n```\n\n### Complete Function Calling Loop\n\n```python\nclass FunctionCaller:\n    def __init__(self, client, functions: dict):\n        self.client = client\n        self.functions = functions\n        self.function_definitions = self._build_definitions()\n    \n    def run(self, user_message: str) -> str:\n        messages = [{\"role\": \"user\", \"content\": user_message}]\n        \n        while True:\n            response = self.client.chat.completions.create(\n                model=\"gpt-4-turbo-preview\",\n                messages=messages,\n                functions=self.function_definitions,\n                function_call=\"auto\"\n            )\n            \n            message = response.choices[0].message\n            messages.append(message)\n            \n            # Check if done\n            if not message.function_call:\n                return message.content\n            \n            # Execute function\n            function_name = message.function_call.name\n            arguments = json.loads(message.function_call.arguments)\n            \n            result = self.execute_function(function_name, arguments)\n            \n            # Add result to messages\n            messages.append({\n                \"role\": \"function\",\n                \"name\": function_name,\n                \"content\": json.dumps(result)\n            })\n    \n    def execute_function(self, name: str, args: dict):\n        if name in self.functions:\n            return self.functions[name](**args)\n        else:\n            return {\"error\": f\"Unknown function: {name}\"}\n    \n    def _build_definitions(self):\n        # Convert function objects to schema\n        pass\n```\n\n### Function Schema Best Practices\n\n#### 1. Clear Descriptions\n```python\n{\n    \"name\": \"calculate_mortgage\",\n    \"description\": \"Calculate monthly mortgage payment\",  # Good\n    # vs\n    \"description\": \"calc mortgage\",  # Bad - too vague\n}\n```\n\n#### 2. Detailed Parameter Descriptions\n```python\n{\n    \"properties\": {\n        \"loan_amount\": {\n            \"type\": \"number\",\n            \"description\": \"Total loan amount in USD, between 50000 and 10000000\"\n        },\n        \"interest_rate\": {\n            \"type\": \"number\",\n            \"description\": \"Annual interest rate as decimal, e.g., 0.065 for 6.5%\"\n        }\n    }\n}\n```\n\n#### 3. Use Enums for Fixed Options\n```python\n{\n    \"properties\": {\n        \"sort_order\": {\n            \"type\": \"string\",\n            \"enum\": [\"asc\", \"desc\"],\n            \"description\": \"Sort order for results\"\n        }\n    }\n}\n```\n\n#### 4. Default Values\n```python\n{\n    \"properties\": {\n        \"limit\": {\n            \"type\": \"integer\",\n            \"default\": 10,\n            \"description\": \"Maximum results to return\"\n        }\n    }\n}\n```\n\n### Tool Use with Claude (Anthropic)\n\n```python\nimport anthropic\n\nclient = anthropic.Anthropic()\n\nmessage = client.messages.create(\n    model=\"claude-3-opus-20240229\",\n    max_tokens=1024,\n    tools=[\n        {\n            \"name\": \"get_weather\",\n            \"description\": \"Get weather for location\",\n            \"input_schema\": {\n                \"type\": \"object\",\n                \"properties\": {\n                    \"location\": {\"type\": \"string\"}\n                },\n                \"required\": [\"location\"]\n            }\n        }\n    ],\n    messages=[\n        {\"role\": \"user\", \"content\": \"What's the weather in Paris?\"}\n    ]\n)\n\nif message.stop_reason == \"tool_use\":\n    for block in message.content:\n        if block.type == \"tool_use\":\n            tool_name = block.name\n            tool_input = block.input\n            # Execute and continue...\n```\n\n### Hands-on Exercise\n\n1. Build a function calling system with 3 tools\n2. Handle multiple function calls in sequence\n3. Add error handling for invalid parameters\n4. Test with various user queries"
        },
        {
          "title": "Building Custom Tools",
          "content": "## Tool Design Principles\n\n### What Makes a Good Tool?\n\n1. **Clear Purpose**: Single, well-defined responsibility\n2. **Simple Interface**: Minimal required parameters\n3. **Robust**: Handles errors gracefully\n4. **Documented**: Clear descriptions for LLM\n\n### Tool Framework\n\n```python\nfrom dataclasses import dataclass\nfrom typing import Callable, Any\nimport json\n\n@dataclass\nclass Tool:\n    name: str\n    description: str\n    parameters: dict\n    function: Callable\n    \n    def to_schema(self) -> dict:\n        \"\"\"Convert to LLM-compatible schema.\"\"\"\n        return {\n            \"name\": self.name,\n            \"description\": self.description,\n            \"parameters\": self.parameters\n        }\n    \n    def execute(self, **kwargs) -> Any:\n        \"\"\"Execute the tool with validation.\"\"\"\n        # Validate parameters\n        self.validate(kwargs)\n        \n        # Execute\n        try:\n            result = self.function(**kwargs)\n            return {\"success\": True, \"result\": result}\n        except Exception as e:\n            return {\"success\": False, \"error\": str(e)}\n    \n    def validate(self, params: dict):\n        \"\"\"Validate parameters against schema.\"\"\"\n        required = self.parameters.get(\"required\", [])\n        for param in required:\n            if param not in params:\n                raise ValueError(f\"Missing required parameter: {param}\")\n```\n\n### Tool Builder Pattern\n\n```python\nclass ToolBuilder:\n    def __init__(self, name: str):\n        self.name = name\n        self.description = \"\"\n        self.params = {\"type\": \"object\", \"properties\": {}, \"required\": []}\n        self.function = None\n    \n    def describe(self, description: str) -> 'ToolBuilder':\n        self.description = description\n        return self\n    \n    def param(self, name: str, type: str, description: str, \n              required: bool = True, enum: list = None, default = None):\n        self.params[\"properties\"][name] = {\n            \"type\": type,\n            \"description\": description\n        }\n        if enum:\n            self.params[\"properties\"][name][\"enum\"] = enum\n        if default is not None:\n            self.params[\"properties\"][name][\"default\"] = default\n        if required:\n            self.params[\"required\"].append(name)\n        return self\n    \n    def implement(self, func: Callable) -> 'ToolBuilder':\n        self.function = func\n        return self\n    \n    def build(self) -> Tool:\n        return Tool(\n            name=self.name,\n            description=self.description,\n            parameters=self.params,\n            function=self.function\n        )\n\n# Usage\nweather_tool = (ToolBuilder(\"get_weather\")\n    .describe(\"Get current weather for a location\")\n    .param(\"location\", \"string\", \"City and country\")\n    .param(\"unit\", \"string\", \"Temperature unit\", \n           enum=[\"celsius\", \"fahrenheit\"], default=\"celsius\")\n    .implement(get_weather_data)\n    .build())\n```\n\n### Example Tools\n\n#### Web Search Tool\n```python\ndef web_search(query: str, num_results: int = 5) -> dict:\n    \"\"\"Search the web.\"\"\"\n    # Implementation using search API\n    results = search_api.search(query, max_results=num_results)\n    \n    return {\n        \"results\": [\n            {\n                \"title\": r.title,\n                \"url\": r.url,\n                \"snippet\": r.snippet\n            }\n            for r in results\n        ]\n    }\n\nweb_search_tool = Tool(\n    name=\"web_search\",\n    description=\"Search the web for information\",\n    parameters={\n        \"type\": \"object\",\n        \"properties\": {\n            \"query\": {\n                \"type\": \"string\",\n                \"description\": \"Search query\"\n            },\n            \"num_results\": {\n                \"type\": \"integer\",\n                \"description\": \"Number of results (1-10)\",\n                \"default\": 5\n            }\n        },\n        \"required\": [\"query\"]\n    },\n    function=web_search\n)\n```\n\n#### Calculator Tool\n```python\ndef calculate(expression: str) -> dict:\n    \"\"\"Safely evaluate mathematical expressions.\"\"\"\n    allowed_chars = set(\"0123456789+-*/.() \")\n    if not all(c in allowed_chars for c in expression):\n        return {\"error\": \"Invalid characters in expression\"}\n    \n    try:\n        result = eval(expression)\n        return {\"result\": result}\n    except Exception as e:\n        return {\"error\": str(e)}\n\ncalculator_tool = Tool(\n    name=\"calculate\",\n    description=\"Evaluate mathematical expressions\",\n    parameters={\n        \"type\": \"object\",\n        \"properties\": {\n            \"expression\": {\n                \"type\": \"string\",\n                \"description\": \"Math expression, e.g., '(2 + 2) * 3'\"\n            }\n        },\n        \"required\": [\"expression\"]\n    },\n    function=calculate\n)\n```\n\n#### Database Query Tool\n```python\ndef query_database(table: str, filters: dict = None, limit: int = 10) -> dict:\n    \"\"\"Query database table.\"\"\"\n    query = f\"SELECT * FROM {table}\"\n    \n    if filters:\n        conditions = [f\"{k} = '{v}'\" for k, v in filters.items()]\n        query += \" WHERE \" + \" AND \".join(conditions)\n    \n    query += f\" LIMIT {limit}\"\n    \n    results = db.execute(query)\n    return {\"rows\": results}\n\ndb_tool = Tool(\n    name=\"query_database\",\n    description=\"Query the database for information\",\n    parameters={\n        \"type\": \"object\",\n        \"properties\": {\n            \"table\": {\n                \"type\": \"string\",\n                \"enum\": [\"users\", \"products\", \"orders\"],\n                \"description\": \"Table to query\"\n            },\n            \"filters\": {\n                \"type\": \"object\",\n                \"description\": \"Filter conditions\"\n            },\n            \"limit\": {\n                \"type\": \"integer\",\n                \"default\": 10\n            }\n        },\n        \"required\": [\"table\"]\n    },\n    function=query_database\n)\n```\n\n### Tool Registry\n\n```python\nclass ToolRegistry:\n    def __init__(self):\n        self.tools = {}\n    \n    def register(self, tool: Tool):\n        self.tools[tool.name] = tool\n    \n    def get(self, name: str) -> Tool:\n        return self.tools.get(name)\n    \n    def get_schemas(self) -> list:\n        return [tool.to_schema() for tool in self.tools.values()]\n    \n    def execute(self, name: str, **kwargs) -> Any:\n        tool = self.get(name)\n        if not tool:\n            return {\"error\": f\"Unknown tool: {name}\"}\n        return tool.execute(**kwargs)\n\n# Usage\nregistry = ToolRegistry()\nregistry.register(weather_tool)\nregistry.register(calculator_tool)\nregistry.register(web_search_tool)\n```\n\n### Error Handling in Tools\n\n```python\nclass ToolResult:\n    def __init__(self, success: bool, data: Any = None, error: str = None):\n        self.success = success\n        self.data = data\n        self.error = error\n    \n    def to_dict(self) -> dict:\n        if self.success:\n            return {\"success\": True, \"data\": self.data}\n        else:\n            return {\"success\": False, \"error\": self.error}\n\ndef safe_tool_execution(func):\n    def wrapper(*args, **kwargs):\n        try:\n            result = func(*args, **kwargs)\n            return ToolResult(success=True, data=result)\n        except ValueError as e:\n            return ToolResult(success=False, error=f\"Invalid input: {e}\")\n        except PermissionError:\n            return ToolResult(success=False, error=\"Permission denied\")\n        except Exception as e:\n            return ToolResult(success=False, error=f\"Unexpected error: {e}\")\n    return wrapper\n```\n\n### Hands-on Exercise\n\n1. Build a tool builder framework\n2. Create 5 different tools for various domains\n3. Implement a tool registry with execution\n4. Add comprehensive error handling"
        },
        {
          "title": "Advanced Tool Patterns",
          "content": "## Multi-Tool Workflows\n\n### Sequential Tool Calls\n```python\nclass ToolChain:\n    \"\"\"Execute tools in sequence.\"\"\"\n    \n    def __init__(self, tools: List[Tool]):\n        self.tools = tools\n    \n    async def execute(self, initial_input: dict) -> dict:\n        result = initial_input\n        \n        for tool in self.tools:\n            result = await tool.execute(**result)\n            if not result.get('success'):\n                break\n        \n        return result\n\n# Example: Search → Scrape → Summarize\nchain = ToolChain([\n    search_tool,\n    scrape_tool,\n    summarize_tool\n])\n\nresult = await chain.execute({\"query\": \"AI news\"})\n```\n\n### Parallel Tool Execution\n```python\nimport asyncio\n\nclass ParallelToolExecutor:\n    \"\"\"Execute multiple tools simultaneously.\"\"\"\n    \n    async def execute_all(self, tool_calls: List[dict]) -> List[dict]:\n        tasks = [\n            self.execute_tool(call['name'], call['arguments'])\n            for call in tool_calls\n        ]\n        return await asyncio.gather(*tasks)\n```\n\n### Conditional Tool Selection\n```python\nclass SmartToolSelector:\n    \"\"\"Select tools based on context.\"\"\"\n    \n    def __init__(self, registry: ToolRegistry, llm):\n        self.registry = registry\n        self.llm = llm\n    \n    def select_tools(self, query: str) -> List[Tool]:\n        prompt = f\"\"\"\n        Query: {query}\n        \n        Available tools:\n        {self.registry.get_descriptions()}\n        \n        Which tools are needed? Return tool names as JSON array.\n        \"\"\"\n        \n        response = self.llm.generate(prompt)\n        tool_names = json.loads(response)\n        \n        return [self.registry.get(name) for name in tool_names]\n```\n\n### Tool Composition\n\n```python\nclass CompositeTool(Tool):\n    \"\"\"Tool composed of multiple tools.\"\"\"\n    \n    def __init__(self, name: str, description: str, subtools: List[Tool]):\n        self.subtools = subtools\n        super().__init__(\n            name=name,\n            description=description,\n            parameters=self._merge_parameters(),\n            function=self._composite_function\n        )\n    \n    def _merge_parameters(self) -> dict:\n        \"\"\"Merge parameters from all subtools.\"\"\"\n        merged = {\"type\": \"object\", \"properties\": {}, \"required\": []}\n        \n        for tool in self.subtools:\n            merged[\"properties\"].update(tool.parameters[\"properties\"])\n            merged[\"required\"].extend(tool.parameters.get(\"required\", []))\n        \n        return merged\n    \n    async def _composite_function(self, **kwargs):\n        \"\"\"Execute all subtools.\"\"\"\n        results = []\n        for tool in self.subtools:\n            # Extract relevant params for this tool\n            tool_params = self._extract_params(tool, kwargs)\n            result = await tool.execute(**tool_params)\n            results.append(result)\n        \n        return self._merge_results(results)\n```\n\n### Tool Output Processing\n\n```python\nclass ToolOutputProcessor:\n    \"\"\"Process and transform tool outputs.\"\"\"\n    \n    @staticmethod\n    def truncate(result: dict, max_length: int = 1000) -> dict:\n        \"\"\"Truncate long outputs.\"\"\"\n        if isinstance(result, str) and len(result) > max_length:\n            return result[:max_length] + \"...[truncated]\"\n        return result\n    \n    @staticmethod\n    def summarize(result: dict, llm) -> dict:\n        \"\"\"Summarize complex outputs.\"\"\"\n        if isinstance(result, str) and len(result) > 500:\n            prompt = f\"Summarize: {result}\"\n            return llm.generate(prompt, max_tokens=100)\n        return result\n    \n    @staticmethod\n    def format_for_llm(result: dict) -> str:\n        \"\"\"Format result for LLM consumption.\"\"\"\n        if isinstance(result, dict):\n            return json.dumps(result, indent=2)\n        return str(result)\n```\n\n### Tool Validation and Safety\n\n```python\nclass ToolValidator:\n    \"\"\"Validate tool calls for safety.\"\"\"\n    \n    def __init__(self):\n        self.rate_limits = {}\n        self.banned_params = {}\n    \n    def validate(self, tool_name: str, params: dict) -> tuple[bool, str]:\n        \"\"\"Validate tool call.\"\"\"\n        \n        # Check rate limits\n        if not self.check_rate_limit(tool_name):\n            return False, \"Rate limit exceeded\"\n        \n        # Check for banned parameters\n        if tool_name in self.banned_params:\n            for banned in self.banned_params[tool_name]:\n                if banned in params:\n                    return False, f\"Parameter '{banned}' is not allowed\"\n        \n        # Validate parameter values\n        if not self.validate_params(tool_name, params):\n            return False, \"Invalid parameters\"\n        \n        return True, \"OK\"\n    \n    def check_rate_limit(self, tool_name: str) -> bool:\n        \"\"\"Check if rate limit allows execution.\"\"\"\n        key = f\"{tool_name}:{datetime.now().minute}\"\n        self.rate_limits[key] = self.rate_limits.get(key, 0) + 1\n        return self.rate_limits[key] < 10  # 10 calls per minute\n```\n\n### Tool State Management\n\n```python\nclass StatefulTool:\n    \"\"\"Tool that maintains state across calls.\"\"\"\n    \n    def __init__(self, name: str):\n        self.name = name\n        self.state = {}\n    \n    def execute(self, **kwargs):\n        # Update state\n        self.state.update(kwargs.get('_state_update', {}))\n        \n        # Use state in execution\n        result = self._execute_with_state(kwargs)\n        \n        # Return result with state\n        return {\n            **result,\n            '_state': self.state\n        }\n\nclass SessionAwareToolRegistry:\n    \"\"\"Registry that maintains tool state per session.\"\"\"\n    \n    def __init__(self):\n        self.sessions = {}  # session_id -> {tool_name -> state}\n    \n    def execute(self, session_id: str, tool_name: str, **kwargs):\n        # Get or create session state\n        if session_id not in self.sessions:\n            self.sessions[session_id] = {}\n        \n        session_tools = self.sessions[session_id]\n        \n        # Restore tool state\n        if tool_name in session_tools:\n            kwargs['_state'] = session_tools[tool_name]\n        \n        # Execute\n        result = self.tools[tool_name].execute(**kwargs)\n        \n        # Save state\n        if '_state' in result:\n            session_tools[tool_name] = result.pop('_state')\n        \n        return result\n```\n\n### Hands-on Exercise\n\n1. Build a tool chain executor\n2. Implement parallel tool execution\n3. Create a stateful tool system\n4. Add validation and rate limiting"
        },
        {
          "title": "Week 5 Project: Tool-Using Agent",
          "content": "## Project: Build a Tool-Using Agent\n\n### Objective\nCreate an agent that can use multiple tools to accomplish complex tasks.\n\n### Requirements\n\n#### 1. Tool System\n```python\nclass ToolSystem:\n    \"\"\"\n    Complete tool management system.\n    \"\"\"\n    \n    def __init__(self):\n        self.registry = ToolRegistry()\n        self.validator = ToolValidator()\n        self.processor = ToolOutputProcessor()\n    \n    def register_tool(self, tool: Tool):\n        pass\n    \n    def execute(self, name: str, params: dict) -> ToolResult:\n        pass\n```\n\n#### 2. Built-in Tools\nImplement these tools:\n\n**Web Tools**\n```python\n- web_search(query, num_results=5) → SearchResults\n- scrape_url(url) → PageContent\n- extract_links(url) → List[URL]\n```\n\n**Data Tools**\n```python\n- calculate(expression) → Number\n- format_data(data, format) → FormattedData\n- analyze_csv(csv_string) → Analysis\n```\n\n**File Tools**\n```python\n- read_file(path) → FileContent\n- write_file(path, content) → Success\n- list_directory(path) → List[File]\n```\n\n**Code Tools**\n```python\n- execute_python(code) → ExecutionResult\n- format_code(code, language) → FormattedCode\n- lint_code(code, language) → LintResults\n```\n\n#### 3. Agent with Tool Use\n```python\nclass ToolUsingAgent:\n    \"\"\"\n    Agent that uses tools intelligently.\n    \"\"\"\n    \n    def __init__(self, llm, tool_system: ToolSystem):\n        self.llm = llm\n        self.tools = tool_system\n        self.memory = AgentMemory()\n    \n    def run(self, task: str) -> str:\n        \"\"\"\n        Execute task using tools.\n        \n        Loop:\n        1. Analyze task and decide on tools\n        2. Call tools with extracted parameters\n        3. Process results\n        4. Generate response or continue\n        \"\"\"\n        pass\n```\n\n### Project Structure\n\n```\ntool_agent/\n├── tools/\n│   ├── __init__.py\n│   ├── base.py\n│   ├── web_tools.py\n│   ├── data_tools.py\n│   ├── file_tools.py\n│   └── code_tools.py\n├── core/\n│   ├── __init__.py\n│   ├── registry.py\n│   ├── validator.py\n│   └── executor.py\n├── agent/\n│   ├── __init__.py\n│   ├── agent.py\n│   ├── planner.py\n│   └── memory.py\n├── utils/\n│   ├── __init__.py\n│   ├── output_processor.py\n│   └── safety.py\n└── cli.py\n```\n\n### Example Interactions\n\n```\nUser: Search for recent AI news and summarize the top 3 articles.\n\nAgent:\n[Thinking: Need to search web and process results]\nCalling: web_search(\"recent AI news 2024\", num_results=5)\nResult: 5 search results\n\n[Thinking: Get content from top 3]\nCalling: scrape_url(results[0].url)\nCalling: scrape_url(results[1].url)\nCalling: scrape_url(results[2].url)\n\n[Thinking: Summarize content]\nGenerating summary...\n\nResponse: Here are the top 3 AI news stories:\n1. [Summary with source]\n2. [Summary with source]\n3. [Summary with source]\n```\n\n```\nUser: Calculate the compound interest on $10,000 at 5% for 10 years.\n\nAgent:\n[Thinking: Need calculation]\nCalling: calculate(\"10000 * (1.05 ** 10)\")\nResult: 16288.95\n\nResponse: The compound interest would be $16,288.95.\n```\n\n### Advanced Features\n\n#### Tool Selection Optimization\n```python\nclass ToolSelector:\n    \"\"\"\n    Intelligently select tools based on task.\n    \"\"\"\n    \n    def select(self, task: str, context: dict) -> List[ToolCall]:\n        \"\"\"\n        Analyze task and return optimal tool calls.\n        Considers:\n        - Task requirements\n        - Available tools\n        - Past performance\n        - Resource constraints\n        \"\"\"\n        pass\n```\n\n#### Error Recovery\n```python\nclass ErrorRecovery:\n    \"\"\"\n    Handle tool failures gracefully.\n    \"\"\"\n    \n    def recover(self, failed_call: ToolCall, error: Exception) -> RecoveryPlan:\n        \"\"\"\n        Generate recovery plan:\n        - Retry with different params\n        - Use alternative tool\n        - Report inability\n        \"\"\"\n        pass\n```\n\n### CLI Tool\n\n```bash\ntool-agent run \"Search for Python tutorials and list the top 5\"\n\n# Output:\n# 🔍 Analyzing task...\n# 🔧 Selected tools: web_search, scrape_url\n# 🌐 Searching web...\n# 📄 Processing results...\n# ✅ Done!\n# \n# Top 5 Python tutorials:\n# 1. Tutorial Name - URL\n# ...\n```\n\n### Deliverables\n\n1. **Working agent** with all tools\n2. **Tool framework** with validation and safety\n3. **CLI tool** for testing\n4. **Tests** for each tool\n5. **Documentation** with examples\n\n### Evaluation Criteria\n\n- Tool implementation (30%)\n- Agent intelligence (25%)\n- Error handling (20%)\n- Code quality (15%)\n- Documentation (10%)\n\n### Extension Ideas\n\n- Custom tool creation API\n- Tool performance monitoring\n- Automatic tool documentation\n- Tool marketplace/sharing\n- Visual tool flow builder"
        }
      ]
    },
    {
      "week": 6,
      "title": "API Integration & Orchestration",
      "description": "Connect AI systems to external services. Build robust API integrations, handle authentication, and orchestrate complex workflows.",
      "lessons": [
        {
          "title": "API Integration Fundamentals",
          "content": "## Working with External APIs\n\n### API Integration Challenges\n\n1. **Authentication**: Various auth methods\n2. **Rate Limits**: Request throttling\n3. **Data Formats**: JSON, XML, custom\n4. **Error Handling**: Network failures, API errors\n5. **Versioning**: API changes over time\n\n### HTTP Client Best Practices\n\n```python\nimport httpx\nfrom typing import Optional\n\nclass APIClient:\n    \"\"\"\n    Robust API client with retries, timeouts, and error handling.\n    \"\"\"\n    \n    def __init__(\n        self,\n        base_url: str,\n        timeout: float = 30.0,\n        max_retries: int = 3\n    ):\n        self.base_url = base_url.rstrip('/')\n        self.timeout = timeout\n        self.max_retries = max_retries\n        self.client = httpx.Client(timeout=timeout)\n    \n    async def request(\n        self,\n        method: str,\n        endpoint: str,\n        **kwargs\n    ) -> httpx.Response:\n        \"\"\"Make HTTP request with retries.\"\"\"\n        url = f\"{self.base_url}/{endpoint.lstrip('/')}\"\n        \n        for attempt in range(self.max_retries):\n            try:\n                response = await self.client.request(method, url, **kwargs)\n                response.raise_for_status()\n                return response\n            except httpx.HTTPStatusError as e:\n                if e.response.status_code < 500:\n                    raise  # Client error, don't retry\n                if attempt == self.max_retries - 1:\n                    raise\n                await asyncio.sleep(2 ** attempt)  # Exponential backoff\n            except httpx.RequestError:\n                if attempt == self.max_retries - 1:\n                    raise\n                await asyncio.sleep(2 ** attempt)\n    \n    async def get(self, endpoint: str, **kwargs):\n        return await self.request('GET', endpoint, **kwargs)\n    \n    async def post(self, endpoint: str, **kwargs):\n        return await self.request('POST', endpoint, **kwargs)\n```\n\n### Authentication Patterns\n\n#### API Key\n```python\nclass APIKeyAuth:\n    def __init__(self, api_key: str, header_name: str = \"X-API-Key\"):\n        self.api_key = api_key\n        self.header_name = header_name\n    \n    def apply(self, headers: dict) -> dict:\n        headers[self.header_name] = self.api_key\n        return headers\n```\n\n#### Bearer Token\n```python\nclass BearerAuth:\n    def __init__(self, token: str):\n        self.token = token\n    \n    def apply(self, headers: dict) -> dict:\n        headers[\"Authorization\"] = f\"Bearer {self.token}\"\n        return headers\n```\n\n#### OAuth 2.0\n```python\nclass OAuth2Auth:\n    def __init__(self, client_id: str, client_secret: str, token_url: str):\n        self.client_id = client_id\n        self.client_secret = client_secret\n        self.token_url = token_url\n        self.token = None\n        self.token_expires = None\n    \n    async def get_token(self) -> str:\n        \"\"\"Get or refresh access token.\"\"\"\n        if self.token and self.token_expires > datetime.now():\n            return self.token\n        \n        async with httpx.AsyncClient() as client:\n            response = await client.post(\n                self.token_url,\n                data={\n                    \"grant_type\": \"client_credentials\",\n                    \"client_id\": self.client_id,\n                    \"client_secret\": self.client_secret\n                }\n            )\n            data = response.json()\n            self.token = data[\"access_token\"]\n            self.token_expires = datetime.now() + timedelta(seconds=data[\"expires_in\"])\n            \n        return self.token\n    \n    async def apply(self, headers: dict) -> dict:\n        token = await self.get_token()\n        headers[\"Authorization\"] = f\"Bearer {token}\"\n        return headers\n```\n\n### Rate Limiting\n\n```python\nclass RateLimiter:\n    \"\"\"\n    Token bucket rate limiter.\n    \"\"\"\n    \n    def __init__(self, rate: float, capacity: int):\n        self.rate = rate  # Tokens per second\n        self.capacity = capacity  # Max tokens\n        self.tokens = capacity\n        self.last_update = time.time()\n        self.lock = asyncio.Lock()\n    \n    async def acquire(self, tokens: int = 1):\n        \"\"\"Wait until tokens are available.\"\"\"\n        async with self.lock:\n            now = time.time()\n            elapsed = now - self.last_update\n            self.tokens = min(\n                self.capacity,\n                self.tokens + elapsed * self.rate\n            )\n            self.last_update = now\n            \n            if self.tokens >= tokens:\n                self.tokens -= tokens\n                return\n            \n            # Wait for tokens\n            wait_time = (tokens - self.tokens) / self.rate\n            await asyncio.sleep(wait_time)\n            self.tokens = 0\n\nclass RateLimitedClient:\n    def __init__(self, base_url: str, requests_per_second: float = 10):\n        self.client = APIClient(base_url)\n        self.limiter = RateLimiter(requests_per_second, int(requests_per_second * 2))\n    \n    async def request(self, method: str, endpoint: str, **kwargs):\n        await self.limiter.acquire()\n        return await self.client.request(method, endpoint, **kwargs)\n```\n\n### Error Handling\n\n```python\nclass APIError(Exception):\n    \"\"\"Base API error.\"\"\"\n    pass\n\nclass RateLimitError(APIError):\n    \"\"\"Rate limit exceeded.\"\"\"\n    def __init__(self, retry_after: int = None):\n        self.retry_after = retry_after\n\nclass AuthenticationError(APIError):\n    \"\"\"Authentication failed.\"\"\"\n    pass\n\nclass NotFoundError(APIError):\n    \"\"\"Resource not found.\"\"\"\n    pass\n\ndef handle_api_error(response: httpx.Response):\n    \"\"\"Convert HTTP errors to API errors.\"\"\"\n    if response.status_code == 401:\n        raise AuthenticationError(\"Invalid credentials\")\n    elif response.status_code == 404:\n        raise NotFoundError(\"Resource not found\")\n    elif response.status_code == 429:\n        retry_after = response.headers.get(\"Retry-After\")\n        raise RateLimitError(retry_after=int(retry_after) if retry_after else None)\n    elif response.status_code >= 500:\n        raise APIError(f\"Server error: {response.status_code}\")\n    elif response.status_code >= 400:\n        raise APIError(f\"Client error: {response.status_code}\")\n```\n\n### Response Processing\n\n```python\nfrom dataclasses import dataclass\nfrom typing import Generic, TypeVar\n\nT = TypeVar('T')\n\n@dataclass\nclass APIResponse(Generic[T]):\n    data: T\n    status_code: int\n    headers: dict\n    raw: httpx.Response\n\nclass ResponseProcessor:\n    @staticmethod\n    def parse_json(response: httpx.Response) -> dict:\n        return response.json()\n    \n    @staticmethod\n    def parse_list(response: httpx.Response, item_class: type) -> list:\n        data = response.json()\n        return [item_class(**item) for item in data]\n    \n    @staticmethod\n    def handle_pagination(\n        response: httpx.Response,\n        client: 'APIClient',\n        endpoint: str\n    ) -> 'PaginatedIterator':\n        return PaginatedIterator(response, client, endpoint)\n\nclass PaginatedIterator:\n    def __init__(self, response, client, endpoint):\n        self.response = response\n        self.client = client\n        self.endpoint = endpoint\n        self.next_cursor = self._extract_cursor(response)\n    \n    def _extract_cursor(self, response):\n        data = response.json()\n        return data.get('next_cursor') or data.get('next_page_token')\n    \n    async def __aiter__(self):\n        while True:\n            yield self.response.json()\n            \n            if not self.next_cursor:\n                break\n            \n            self.response = await self.client.get(\n                self.endpoint,\n                params={'cursor': self.next_cursor}\n            )\n            self.next_cursor = self._extract_cursor(self.response)\n```\n\n### Hands-on Exercise\n\n1. Build a robust API client with auth and rate limiting\n2. Implement OAuth2 flow for a popular API\n3. Create pagination handling\n4. Add comprehensive error handling"
        },
        {
          "title": "Orchestrating Multiple Services",
          "content": "## Service Orchestration\n\n### Orchestration Patterns\n\n#### 1. Sequential Orchestration\n```python\nclass SequentialOrchestrator:\n    \"\"\"\n    Execute services in sequence.\n    \"\"\"\n    \n    def __init__(self, services: List[Service]):\n        self.services = services\n    \n    async def execute(self, input_data: dict) -> dict:\n        result = input_data\n        \n        for service in self.services:\n            result = await service.process(result)\n            \n            if not result.get('success'):\n                # Handle failure\n                result = await self.handle_failure(service, result)\n                if not result.get('success'):\n                    break\n        \n        return result\n```\n\n#### 2. Parallel Orchestration\n```python\nclass ParallelOrchestrator:\n    \"\"\"\n    Execute services in parallel.\n    \"\"\"\n    \n    def __init__(self, services: List[Service]):\n        self.services = services\n    \n    async def execute(self, input_data: dict) -> dict:\n        tasks = [\n            service.process(input_data)\n            for service in self.services\n        ]\n        \n        results = await asyncio.gather(*tasks, return_exceptions=True)\n        \n        return self.merge_results(results)\n    \n    def merge_results(self, results: List[dict]) -> dict:\n        merged = {}\n        for i, result in enumerate(results):\n            if isinstance(result, Exception):\n                merged[f\"service_{i}_error\"] = str(result)\n            else:\n                merged[f\"service_{i}\"] = result\n        return merged\n```\n\n#### 3. Conditional Orchestration\n```python\nclass ConditionalOrchestrator:\n    \"\"\"\n    Route to services based on conditions.\n    \"\"\"\n    \n    def __init__(self):\n        self.routes = []\n    \n    def add_route(self, condition: Callable, service: Service):\n        self.routes.append((condition, service))\n    \n    async def execute(self, input_data: dict) -> dict:\n        for condition, service in self.routes:\n            if condition(input_data):\n                return await service.process(input_data)\n        \n        raise ValueError(\"No matching route\")\n```\n\n### Workflow Engine\n\n```python\nfrom enum import Enum\nfrom dataclasses import dataclass\n\nclass StepStatus(Enum):\n    PENDING = \"pending\"\n    RUNNING = \"running\"\n    COMPLETED = \"completed\"\n    FAILED = \"failed\"\n\n@dataclass\nclass WorkflowStep:\n    name: str\n    service: Service\n    dependencies: List[str]\n    status: StepStatus = StepStatus.PENDING\n    result: dict = None\n\nclass WorkflowEngine:\n    \"\"\"\n    Execute complex workflows with dependencies.\n    \"\"\"\n    \n    def __init__(self):\n        self.steps = {}\n        self.results = {}\n    \n    def add_step(self, name: str, service: Service, dependencies: List[str] = None):\n        self.steps[name] = WorkflowStep(\n            name=name,\n            service=service,\n            dependencies=dependencies or []\n        )\n    \n    async def execute(self, input_data: dict) -> dict:\n        \"\"\"Execute workflow respecting dependencies.\"\"\"\n        while not self.is_complete():\n            # Find ready steps\n            ready = self.get_ready_steps()\n            \n            if not ready:\n                if not self.is_complete():\n                    raise RuntimeError(\"Deadlock detected\")\n                break\n            \n            # Execute ready steps\n            tasks = [self.execute_step(step, input_data) for step in ready]\n            await asyncio.gather(*tasks)\n        \n        return self.results\n    \n    def get_ready_steps(self) -> List[WorkflowStep]:\n        \"\"\"Get steps ready to execute.\"\"\"\n        ready = []\n        for step in self.steps.values():\n            if step.status != StepStatus.PENDING:\n                continue\n            \n            if all(\n                self.steps[dep].status == StepStatus.COMPLETED\n                for dep in step.dependencies\n            ):\n                ready.append(step)\n        \n        return ready\n    \n    async def execute_step(self, step: WorkflowStep, input_data: dict):\n        \"\"\"Execute a single step.\"\"\"\n        step.status = StepStatus.RUNNING\n        \n        try:\n            # Gather inputs from dependencies\n            step_input = {**input_data}\n            for dep in step.dependencies:\n                step_input.update(self.results.get(dep, {}))\n            \n            # Execute\n            result = await step.service.process(step_input)\n            step.result = result\n            self.results[step.name] = result\n            step.status = StepStatus.COMPLETED\n        except Exception as e:\n            step.status = StepStatus.FAILED\n            self.results[step.name] = {\"error\": str(e)}\n            raise\n```\n\n### Service Mesh Pattern\n\n```python\nclass ServiceMesh:\n    \"\"\"\n    Manage multiple services with discovery and routing.\n    \"\"\"\n    \n    def __init__(self):\n        self.services = {}\n        self.middlewares = []\n    \n    def register(self, name: str, service: Service):\n        self.services[name] = service\n    \n    def add_middleware(self, middleware: Callable):\n        self.middlewares.append(middleware)\n    \n    async def call(self, service_name: str, method: str, **kwargs):\n        \"\"\"Call service with middleware chain.\"\"\"\n        service = self.services.get(service_name)\n        if not service:\n            raise ValueError(f\"Unknown service: {service_name}\")\n        \n        # Build middleware chain\n        handler = getattr(service, method)\n        for middleware in reversed(self.middlewares):\n            handler = middleware(handler)\n        \n        return await handler(**kwargs)\n\n# Middleware examples\nasync def logging_middleware(handler):\n    async def wrapper(*args, **kwargs):\n        logger.info(f\"Calling {handler.__name__}\")\n        result = await handler(*args, **kwargs)\n        logger.info(f\"Completed {handler.__name__}\")\n        return result\n    return wrapper\n\nasync def retry_middleware(max_retries=3):\n    async def middleware(handler):\n        async def wrapper(*args, **kwargs):\n            for attempt in range(max_retries):\n                try:\n                    return await handler(*args, **kwargs)\n                except Exception as e:\n                    if attempt == max_retries - 1:\n                        raise\n                    await asyncio.sleep(2 ** attempt)\n        return wrapper\n    return middleware\n```\n\n### Data Flow Orchestration\n\n```python\nclass DataPipeline:\n    \"\"\"\n    Orchestrate data through transformations.\n    \"\"\"\n    \n    def __init__(self):\n        self.stages = []\n    \n    def source(self, fetcher: Callable):\n        self.stages.append(('source', fetcher))\n        return self\n    \n    def transform(self, transformer: Callable):\n        self.stages.append(('transform', transformer))\n        return self\n    \n    def sink(self, writer: Callable):\n        self.stages.append(('sink', writer))\n        return self\n    \n    async def execute(self):\n        data = None\n        \n        for stage_type, stage_func in self.stages:\n            if stage_type == 'source':\n                data = await stage_func()\n            elif stage_type == 'transform':\n                if isinstance(data, list):\n                    data = [await stage_func(item) for item in data]\n                else:\n                    data = await stage_func(data)\n            elif stage_type == 'sink':\n                await stage_func(data)\n        \n        return data\n\n# Usage\npipeline = (DataPipeline()\n    .source(fetch_from_api)\n    .transform(parse_json)\n    .transform(extract_fields)\n    .sink(save_to_database))\n\nawait pipeline.execute()\n```\n\n### Hands-on Exercise\n\n1. Build a workflow engine with dependency management\n2. Implement a service mesh with middleware\n3. Create a data pipeline orchestrator\n4. Handle failures and retries"
        },
        {
          "title": "Building Integration Layers",
          "content": "## Integration Architecture\n\n### Integration Layer Design\n\n```python\nclass IntegrationLayer:\n    \"\"\"\n    Unified interface for external services.\n    \"\"\"\n    \n    def __init__(self):\n        self.integrations = {}\n        self.transformers = {}\n        self.cache = Cache()\n    \n    def register(\n        self,\n        name: str,\n        client: ServiceClient,\n        transformer: DataTransformer = None\n    ):\n        self.integrations[name] = client\n        if transformer:\n            self.transformers[name] = transformer\n    \n    async def call(\n        self,\n        integration: str,\n        operation: str,\n        **kwargs\n    ) -> dict:\n        \"\"\"Call integration with caching and transformation.\"\"\"\n        # Check cache\n        cache_key = self._cache_key(integration, operation, kwargs)\n        cached = await self.cache.get(cache_key)\n        if cached:\n            return cached\n        \n        # Call service\n        client = self.integrations[integration]\n        result = await getattr(client, operation)(**kwargs)\n        \n        # Transform if needed\n        if integration in self.transformers:\n            result = self.transformers[integration].transform(result)\n        \n        # Cache result\n        await self.cache.set(cache_key, result)\n        \n        return result\n```\n\n### Common Integration Patterns\n\n#### CRM Integration\n```python\nclass CRMIntegration:\n    \"\"\"\n    Integration with CRM systems.\n    \"\"\"\n    \n    def __init__(self, api_client):\n        self.client = api_client\n    \n    async def create_contact(self, contact: Contact) -> str:\n        \"\"\"Create contact in CRM.\"\"\"\n        response = await self.client.post(\n            \"/contacts\",\n            json={\n                \"email\": contact.email,\n                \"first_name\": contact.first_name,\n                \"last_name\": contact.last_name,\n                \"company\": contact.company\n            }\n        )\n        return response.json()[\"id\"]\n    \n    async def get_contact(self, contact_id: str) -> Contact:\n        \"\"\"Retrieve contact from CRM.\"\"\"\n        response = await self.client.get(f\"/contacts/{contact_id}\")\n        data = response.json()\n        return Contact(\n            id=data[\"id\"],\n            email=data[\"email\"],\n            first_name=data[\"first_name\"],\n            last_name=data[\"last_name\"]\n        )\n    \n    async def search_contacts(self, query: str) -> List[Contact]:\n        \"\"\"Search contacts.\"\"\"\n        response = await self.client.get(\n            \"/contacts/search\",\n            params={\"q\": query}\n        )\n        return [self._parse_contact(c) for c in response.json()]\n```\n\n#### Payment Integration\n```python\nclass PaymentIntegration:\n    \"\"\"\n    Integration with payment providers.\n    \"\"\"\n    \n    def __init__(self, api_client, webhook_handler):\n        self.client = api_client\n        self.webhook_handler = webhook_handler\n    \n    async def create_payment(self, payment: PaymentRequest) -> PaymentResult:\n        \"\"\"Create payment.\"\"\"\n        response = await self.client.post(\n            \"/payments\",\n            json={\n                \"amount\": payment.amount,\n                \"currency\": payment.currency,\n                \"customer_id\": payment.customer_id,\n                \"metadata\": payment.metadata\n            }\n        )\n        return PaymentResult(**response.json())\n    \n    async def handle_webhook(self, payload: dict, signature: str):\n        \"\"\"Handle payment webhook.\"\"\"\n        # Verify signature\n        if not self.verify_webhook(payload, signature):\n            raise ValueError(\"Invalid webhook signature\")\n        \n        # Process event\n        event = payload[\"event\"]\n        await self.webhook_handler.handle(event)\n```\n\n#### Email Integration\n```python\nclass EmailIntegration:\n    \"\"\"\n    Integration with email services.\n    \"\"\"\n    \n    def __init__(self, api_client):\n        self.client = api_client\n    \n    async def send_email(self, email: Email) -> str:\n        \"\"\"Send email.\"\"\"\n        response = await self.client.post(\n            \"/send\",\n            json={\n                \"to\": email.to,\n                \"subject\": email.subject,\n                \"html\": email.html_body,\n                \"text\": email.text_body,\n                \"attachments\": email.attachments\n            }\n        )\n        return response.json()[\"message_id\"]\n    \n    async def send_template(\n        self,\n        template_id: str,\n        to: str,\n        variables: dict\n    ) -> str:\n        \"\"\"Send templated email.\"\"\"\n        response = await self.client.post(\n            \"/send/template\",\n            json={\n                \"template_id\": template_id,\n                \"to\": to,\n                \"variables\": variables\n            }\n        )\n        return response.json()[\"message_id\"]\n```\n\n### Event-Driven Integration\n\n```python\nclass EventDrivenIntegration:\n    \"\"\"\n    Integration using event bus.\n    \"\"\"\n    \n    def __init__(self, event_bus):\n        self.event_bus = event_bus\n        self.handlers = {}\n    \n    def subscribe(self, event_type: str, handler: Callable):\n        \"\"\"Subscribe to events.\"\"\"\n        if event_type not in self.handlers:\n            self.handlers[event_type] = []\n        self.handlers[event_type].append(handler)\n        \n        self.event_bus.subscribe(event_type, self.handle_event)\n    \n    async def handle_event(self, event: Event):\n        \"\"\"Handle incoming event.\"\"\"\n        handlers = self.handlers.get(event.type, [])\n        for handler in handlers:\n            await handler(event.data)\n    \n    async def emit(self, event_type: str, data: dict):\n        \"\"\"Emit event.\"\"\"\n        await self.event_bus.publish(Event(type=event_type, data=data))\n```\n\n### Integration Testing\n\n```python\nclass IntegrationTestSuite:\n    \"\"\"\n    Test integrations with mocks.\n    \"\"\"\n    \n    def __init__(self, integration: Integration):\n        self.integration = integration\n    \n    @pytest.fixture\n    def mock_service(self):\n        \"\"\"Mock external service.\"\"\"\n        with mock.patch('external_service.Client') as mock_client:\n            yield mock_client\n    \n    @pytest.mark.asyncio\n    async def test_create_contact(self, mock_service):\n        \"\"\"Test contact creation.\"\"\"\n        mock_service.post.return_value = mock.Mock(\n            json=lambda: {\"id\": \"123\"}\n        )\n        \n        contact = Contact(\n            email=\"test@example.com\",\n            first_name=\"John\",\n            last_name=\"Doe\"\n        )\n        \n        result = await self.integration.create_contact(contact)\n        \n        assert result == \"123\"\n        mock_service.post.assert_called_once()\n```\n\n### Integration Monitoring\n\n```python\nclass IntegrationMonitor:\n    \"\"\"\n    Monitor integration health and performance.\n    \"\"\"\n    \n    def __init__(self):\n        self.metrics = {}\n    \n    async def track_call(\n        self,\n        integration: str,\n        operation: str,\n        handler: Callable\n    ):\n        \"\"\"Track integration call.\"\"\"\n        start_time = time.time()\n        \n        try:\n            result = await handler()\n            status = \"success\"\n            return result\n        except Exception as e:\n            status = \"error\"\n            raise\n        finally:\n            duration = time.time() - start_time\n            \n            self.record_metric(\n                integration,\n                operation,\n                status,\n                duration\n            )\n    \n    def record_metric(self, integration, operation, status, duration):\n        key = f\"{integration}.{operation}\"\n        if key not in self.metrics:\n            self.metrics[key] = {\n                \"calls\": 0,\n                \"errors\": 0,\n                \"total_duration\": 0\n            }\n        \n        self.metrics[key][\"calls\"] += 1\n        if status == \"error\":\n            self.metrics[key][\"errors\"] += 1\n        self.metrics[key][\"total_duration\"] += duration\n```\n\n### Hands-on Exercise\n\n1. Build an integration layer for 3 services\n2. Implement event-driven integration\n3. Create integration tests with mocks\n4. Add monitoring and health checks"
        },
        {
          "title": "Week 6 Project: AI Integration Hub",
          "content": "## Project: Build an AI Integration Hub\n\n### Objective\nCreate a centralized hub for integrating AI with multiple external services.\n\n### Requirements\n\n#### 1. Core Hub\n```python\nclass AIIntegrationHub:\n    \"\"\"\n    Central hub for AI-service integrations.\n    \"\"\"\n    \n    def __init__(self, llm, config: dict):\n        self.llm = llm\n        self.integrations = IntegrationRegistry()\n        self.orchestrator = ServiceOrchestrator()\n        self.event_bus = EventBus()\n    \n    async def process_request(\n        self,\n        user_request: str,\n        context: dict = None\n    ) -> dict:\n        \"\"\"\n        Process user request using integrations.\n        \n        1. Understand request (LLM)\n        2. Plan integrations needed\n        3. Execute integrations\n        4. Synthesize response\n        \"\"\"\n        pass\n```\n\n#### 2. Built-in Integrations\n\n**Communication**\n```python\n- SlackIntegration: Send/receive messages\n- EmailIntegration: Send emails\n- DiscordIntegration: Bot interactions\n```\n\n**Data Sources**\n```python\n- DatabaseIntegration: Query databases\n- APIIntegration: Call REST APIs\n- GraphQLIntegration: Query GraphQL\n```\n\n**AI Services**\n```python\n- OpenAIIntegration: GPT models\n- AnthropicIntegration: Claude models\n- TranscriptionIntegration: Speech-to-text\n```\n\n**Productivity**\n```python\n- CalendarIntegration: Manage calendar\n- TaskIntegration: Manage tasks\n- StorageIntegration: Cloud storage\n```\n\n#### 3. Integration Builder\n```python\nclass IntegrationBuilder:\n    \"\"\"\n    Build integrations from config.\n    \"\"\"\n    \n    def from_config(self, config: dict) -> Integration:\n        \"\"\"Create integration from configuration.\"\"\"\n        pass\n    \n    def from_openapi(self, spec_url: str) -> Integration:\n        \"\"\"Create integration from OpenAPI spec.\"\"\"\n        pass\n    \n    def from_template(self, template_name: str) -> Integration:\n        \"\"\"Create from predefined template.\"\"\"\n        pass\n```\n\n### Project Structure\n\n```\nintegration_hub/\n├── core/\n│   ├── __init__.py\n│   ├── hub.py\n│   ├── registry.py\n│   └── orchestrator.py\n├── integrations/\n│   ├── __init__.py\n│   ├── base.py\n│   ├── communication/\n│   │   ├── slack.py\n│   │   ├── email.py\n│   │   └── discord.py\n│   ├── data/\n│   │   ├── database.py\n│   │   ├── api.py\n│   │   └── graphql.py\n│   ├── ai/\n│   │   ├── openai.py\n│   │   ├── anthropic.py\n│   │   └── transcription.py\n│   └── productivity/\n│       ├── calendar.py\n│       ├── tasks.py\n│       └── storage.py\n├── orchestration/\n│   ├── __init__.py\n│   ├── planner.py\n│   ├── executor.py\n│   └── workflow.py\n├── events/\n│   ├── __init__.py\n│   ├── bus.py\n│   └── handlers.py\n├── api/\n│   ├── __init__.py\n│   ├── routes.py\n│   └── schemas.py\n└── cli.py\n```\n\n### Example Usage\n\n```python\n# Initialize hub\nhub = AIIntegrationHub(\n    llm=OpenAI(),\n    config={\n        \"slack\": {\"token\": \"xoxb-...\"},\n        \"database\": {\"url\": \"postgresql://...\"},\n        \"calendar\": {\"credentials\": \"...\"}\n    }\n)\n\n# Process request\nresult = await hub.process_request(\n    \"Check my calendar for tomorrow and send a summary to #general on Slack\"\n)\n\n# Output:\n# {\n#   \"calendar_events\": [...],\n#   \"slack_message_id\": \"12345\",\n#   \"summary\": \"Sent calendar summary to #general\"\n# }\n```\n\n### API Endpoints\n\n```python\n# Process request\nPOST /api/process\n{\n  \"request\": \"string\",\n  \"context\": {...},\n  \"integrations\": [\"slack\", \"calendar\"]\n}\n\n# List integrations\nGET /api/integrations\n\n# Get integration status\nGET /api/integrations/{name}/status\n\n# Execute specific integration\nPOST /api/integrations/{name}/execute\n{\n  \"operation\": \"send_message\",\n  \"params\": {...}\n}\n\n# Webhook endpoint\nPOST /api/webhooks/{integration}\n```\n\n### Deliverables\n\n1. **Working hub** with 6+ integrations\n2. **Integration builder** from config/OpenAPI\n3. **CLI tool**:\n   ```bash\n   integration-hub process \"Send email to team\"\n   integration-hub list-integrations\n   integration-hub test slack\n   ```\n4. **Docker setup** for deployment\n5. **Tests** with mocked services\n6. **Documentation** for adding integrations\n\n### Evaluation Criteria\n\n- Integration quality (25%)\n- Orchestration intelligence (25%)\n- Code quality (20%)\n- Documentation (15%)\n- Testing (15%)\n\n### Extension Ideas\n\n- Visual integration builder\n- Integration marketplace\n- Real-time monitoring dashboard\n- Custom integration SDK\n- Multi-tenant support"
        }
      ]
    },
    {
      "week": 7,
      "title": "Testing & Evaluation",
      "description": "Ensure AI system quality through comprehensive testing, evaluation frameworks, and monitoring. Learn to measure and improve performance.",
      "lessons": [
        {
          "title": "Testing AI Systems",
          "content": "## Testing Challenges in AI\n\n### Why AI Testing is Different\n\n1. **Non-deterministic**: Same input ≠ same output\n2. **Context-dependent**: Performance varies by use case\n3. **Emergent behaviors**: Unexpected outputs\n4. **Evaluation complexity**: What's \"correct\"?\n\n### Types of AI Tests\n\n#### 1. Unit Tests for AI Components\n```python\ndef test_embedding_generation():\n    \"\"\"Test embedding model.\"\"\"\n    embedder = EmbeddingModel()\n    \n    text = \"This is a test sentence.\"\n    embedding = embedder.embed(text)\n    \n    assert len(embedding) == 768  # Correct dimension\n    assert all(isinstance(x, float) for x in embedding)\n    assert abs(sum(x**2 for x in embedding) - 1) < 0.01  # Normalized\n\ndef test_chunking():\n    \"\"\"Test document chunking.\"\"\"\n    chunker = DocumentChunker(chunk_size=100)\n    \n    text = \"Word \" * 200\n    chunks = chunker.chunk(text)\n    \n    assert all(len(c.split()) <= 100 for c in chunks)\n    assert len(chunks) > 1\n\ndef test_prompt_template():\n    \"\"\"Test prompt template rendering.\"\"\"\n    template = PromptTemplate(\n        \"Hello {name}, your task is {task}\"\n    )\n    \n    result = template.render(name=\"Alice\", task=\"testing\")\n    \n    assert result == \"Hello Alice, your task is testing\"\n    with pytest.raises(ValueError):\n        template.render(name=\"Alice\")  # Missing task\n```\n\n#### 2. Integration Tests\n```python\n@pytest.mark.asyncio\nasync def test_rag_pipeline():\n    \"\"\"Test complete RAG pipeline.\"\"\"\n    # Setup\n    rag = RAGSystem(\n        embedder=EmbeddingModel(),\n        vector_db=VectorDB(),\n        llm=MockLLM()\n    )\n    \n    # Index documents\n    await rag.index([\n        Document(content=\"Python is a programming language.\"),\n        Document(content=\"JavaScript is also a programming language.\")\n    ])\n    \n    # Query\n    result = await rag.query(\"What is Python?\")\n    \n    assert result.answer is not None\n    assert len(result.sources) > 0\n    assert \"programming language\" in result.answer.lower()\n\n@pytest.mark.asyncio\nasync def test_agent_tool_execution():\n    \"\"\"Test agent using tools.\"\"\"\n    agent = Agent(\n        llm=LLM(),\n        tools=[calculator_tool, search_tool]\n    )\n    \n    result = await agent.run(\"What is 2 + 2?\")\n    \n    assert \"4\" in result\n    assert agent.tool_calls_count > 0\n```\n\n#### 3. Evaluation Tests\n```python\ndef test_classification_accuracy():\n    \"\"\"Test classification model accuracy.\"\"\"\n    classifier = TextClassifier()\n    \n    test_cases = [\n        (\"I love this!\", \"positive\"),\n        (\"This is terrible\", \"negative\"),\n        (\"It's okay\", \"neutral\"),\n    ]\n    \n    correct = 0\n    for text, expected in test_cases:\n        result = classifier.classify(text)\n        if result.label == expected:\n            correct += 1\n    \n    accuracy = correct / len(test_cases)\n    assert accuracy >= 0.8  # 80% threshold\n\ndef test_retrieval_quality():\n    \"\"\"Test retrieval relevance.\"\"\"\n    retriever = Retriever()\n    \n    # Index test documents\n    retriever.index([\n        {\"id\": \"1\", \"text\": \"Python tutorial\"},\n        {\"id\": \"2\", \"text\": \"JavaScript guide\"},\n        {\"id\": \"3\", \"text\": \"Python advanced\"},\n    ])\n    \n    # Query and check relevance\n    results = retriever.retrieve(\"Python programming\", k=2)\n    \n    # Both results should be Python-related\n    assert all(\"Python\" in r.text for r in results)\n```\n\n### Testing Strategies\n\n#### Deterministic Testing\n```python\nclass DeterministicLLM:\n    \"\"\"Mock LLM for deterministic tests.\"\"\"\n    \n    def __init__(self, responses: dict):\n        self.responses = responses\n    \n    def generate(self, prompt: str) -> str:\n        # Match prompt pattern to response\n        for pattern, response in self.responses.items():\n            if pattern in prompt:\n                return response\n        return \"Default response\"\n\n# Usage\ndef test_with_mock():\n    mock_llm = DeterministicLLM({\n        \"sentiment\": \"positive\",\n        \"summary\": \"This is a summary.\"\n    })\n    \n    result = mock_llm.generate(\"What is the sentiment?\")\n    assert result == \"positive\"\n```\n\n#### Probabilistic Testing\n```python\ndef test_response_quality():\n    \"\"\"Test quality over multiple runs.\"\"\"\n    system = AISystem()\n    \n    results = []\n    for _ in range(10):  # Run 10 times\n        response = system.generate(\"Test prompt\")\n        quality = evaluate_quality(response)\n        results.append(quality)\n    \n    avg_quality = sum(results) / len(results)\n    assert avg_quality >= 0.7  # Average quality threshold\n    \n    consistency = statistics.stdev(results)\n    assert consistency < 0.2  # Low variance\n```\n\n#### Property-Based Testing\n```python\nfrom hypothesis import given, strategies as st\n\n@given(st.text(min_size=10, max_size=1000))\ndef test_chunking_properties(text: str):\n    \"\"\"Test chunking invariants.\"\"\"\n    chunker = DocumentChunker(chunk_size=100)\n    chunks = chunker.chunk(text)\n    \n    # Property: Concatenation covers original\n    concatenated = ' '.join(chunks)\n    assert all(word in concatenated for word in text.split())\n    \n    # Property: All chunks respect size limit\n    assert all(len(c.split()) <= 100 for c in chunks)\n\n@given(st.lists(st.text(), min_size=1, max_size=10))\ndef test_embedding_properties(texts: list):\n    \"\"\"Test embedding properties.\"\"\"\n    embedder = EmbeddingModel()\n    embeddings = [embedder.embed(t) for t in texts]\n    \n    # Property: All same dimension\n    assert all(len(e) == len(embeddings[0]) for e in embeddings)\n    \n    # Property: Normalized\n    for emb in embeddings:\n        norm = sum(x**2 for x in emb) ** 0.5\n        assert abs(norm - 1) < 0.01\n```\n\n### Test Fixtures and Data\n\n```python\n@pytest.fixture\ndef sample_documents():\n    \"\"\"Sample documents for testing.\"\"\"\n    return [\n        Document(\n            id=\"1\",\n            content=\"Machine learning is a subset of AI.\",\n            metadata={\"source\": \"wiki\"}\n        ),\n        Document(\n            id=\"2\",\n            content=\"Neural networks power deep learning.\",\n            metadata={\"source\": \"wiki\"}\n        ),\n    ]\n\n@pytest.fixture\ndef qa_test_cases():\n    \"\"\"QA test cases with expected answers.\"\"\"\n    return [\n        {\n            \"question\": \"What is ML?\",\n            \"expected_keywords\": [\"subset\", \"AI\", \"learning\"],\n            \"min_confidence\": 0.7\n        },\n        {\n            \"question\": \"What powers deep learning?\",\n            \"expected_keywords\": [\"neural\", \"networks\"],\n            \"min_confidence\": 0.8\n        }\n    ]\n\n@pytest.mark.asyncio\nasync def test_qa_system(sample_documents, qa_test_cases):\n    \"\"\"Test QA system with fixtures.\"\"\"\n    qa = QASystem()\n    await qa.index(sample_documents)\n    \n    for case in qa_test_cases:\n        result = await qa.answer(case[\"question\"])\n        \n        # Check keywords present\n        assert any(\n            kw.lower() in result.answer.lower()\n            for kw in case[\"expected_keywords\"]\n        )\n        \n        # Check confidence\n        assert result.confidence >= case[\"min_confidence\"]\n```\n\n### Continuous Testing\n\n```python\nclass RegressionTestSuite:\n    \"\"\"\n    Regression tests for AI systems.\n    \"\"\"\n    \n    def __init__(self):\n        self.baselines = self.load_baselines()\n    \n    def test_no_regression(self):\n        \"\"\"Ensure performance hasn't degraded.\"\"\"\n        current_results = self.run_evaluation()\n        \n        for metric, current_value in current_results.items():\n            baseline = self.baselines[metric]\n            \n            # Allow 5% degradation\n            assert current_value >= baseline * 0.95, \\\n                f\"Regression in {metric}: {current_value} < {baseline}\"\n```\n\n### Hands-on Exercise\n\n1. Write unit tests for a RAG system\n2. Create property-based tests for embeddings\n3. Build a regression test suite\n4. Implement test fixtures for common scenarios"
        },
        {
          "title": "Evaluation Frameworks",
          "content": "## AI Evaluation Metrics\n\n### Evaluation Categories\n\n1. **Intrinsic**: Model quality metrics\n2. **Extrinsic**: Task performance metrics\n3. **Human**: Human judgment metrics\n\n### Evaluation Metrics\n\n#### Retrieval Evaluation\n```python\nclass RetrievalEvaluator:\n    \"\"\"\n    Evaluate retrieval quality.\n    \"\"\"\n    \n    def precision_at_k(\n        self,\n        retrieved: List[str],\n        relevant: Set[str],\n        k: int\n    ) -> float:\n        \"\"\"Precision@K: Relevant in top K / K.\"\"\"\n        top_k = retrieved[:k]\n        relevant_in_k = sum(1 for r in top_k if r in relevant)\n        return relevant_in_k / k\n    \n    def recall_at_k(\n        self,\n        retrieved: List[str],\n        relevant: Set[str],\n        k: int\n    ) -> float:\n        \"\"\"Recall@K: Relevant in top K / Total relevant.\"\"\"\n        top_k = retrieved[:k]\n        relevant_in_k = sum(1 for r in top_k if r in relevant)\n        return relevant_in_k / len(relevant)\n    \n    def mrr(\n        self,\n        retrieved: List[str],\n        relevant: Set[str]\n    ) -> float:\n        \"\"\"Mean Reciprocal Rank.\"\"\"\n        for i, r in enumerate(retrieved):\n            if r in relevant:\n                return 1 / (i + 1)\n        return 0\n    \n    def ndcg_at_k(\n        self,\n        retrieved: List[str],\n        relevance_scores: Dict[str, int],\n        k: int\n    ) -> float:\n        \"\"\"Normalized Discounted Cumulative Gain.\"\"\"\n        dcg = sum(\n            relevance_scores.get(r, 0) / np.log2(i + 2)\n            for i, r in enumerate(retrieved[:k])\n        )\n        \n        ideal = sorted(relevance_scores.values(), reverse=True)[:k]\n        idcg = sum(r / np.log2(i + 2) for i, r in enumerate(ideal))\n        \n        return dcg / idcg if idcg > 0 else 0\n```\n\n#### Generation Evaluation\n```python\nclass GenerationEvaluator:\n    \"\"\"\n    Evaluate text generation quality.\n    \"\"\"\n    \n    def __init__(self):\n        self.bleu = BLEUScore()\n        self.rouge = ROUGEScore()\n    \n    def semantic_similarity(\n        self,\n        generated: str,\n        reference: str\n    ) -> float:\n        \"\"\"Semantic similarity using embeddings.\"\"\"\n        gen_emb = self.embed(generated)\n        ref_emb = self.embed(reference)\n        \n        return cosine_similarity(gen_emb, ref_emb)\n    \n    def factuality_score(\n        self,\n        generated: str,\n        sources: List[str]\n    ) -> float:\n        \"\"\"Check factual consistency with sources.\"\"\"\n        # Extract claims from generated\n        claims = self.extract_claims(generated)\n        \n        # Verify each claim against sources\n        verified = 0\n        for claim in claims:\n            if self.verify_claim(claim, sources):\n                verified += 1\n        \n        return verified / len(claims) if claims else 0\n    \n    def coherence_score(self, text: str) -> float:\n        \"\"\"Evaluate text coherence.\"\"\"\n        sentences = self.split_sentences(text)\n        \n        # Check sentence-to-sentence coherence\n        coherence_scores = []\n        for i in range(len(sentences) - 1):\n            score = self.semantic_similarity(\n                sentences[i],\n                sentences[i + 1]\n            )\n            coherence_scores.append(score)\n        \n        return np.mean(coherence_scores) if coherence_scores else 1.0\n```\n\n### Evaluation Datasets\n\n```python\nclass EvaluationDataset:\n    \"\"\"\n    Manage evaluation datasets.\n    \"\"\"\n    \n    def __init__(self, name: str):\n        self.name = name\n        self.samples = []\n    \n    def add_sample(\n        self,\n        input: str,\n        expected_output: str = None,\n        expected_retrieval: List[str] = None,\n        metadata: dict = None\n    ):\n        self.samples.append({\n            \"input\": input,\n            \"expected_output\": expected_output,\n            \"expected_retrieval\": expected_retrieval,\n            \"metadata\": metadata or {}\n        })\n    \n    def from_jsonl(self, path: str):\n        \"\"\"Load from JSONL file.\"\"\"\n        with open(path) as f:\n            for line in f:\n                data = json.loads(line)\n                self.add_sample(**data)\n    \n    def split(self, ratio: float = 0.8):\n        \"\"\"Split into train/test.\"\"\"\n        split_point = int(len(self.samples) * ratio)\n        return (\n            self.samples[:split_point],\n            self.samples[split_point:]\n        )\n```\n\n### Automated Evaluation\n\n```python\nclass AutoEvaluator:\n    \"\"\"\n    Automated evaluation using LLM-as-judge.\n    \"\"\"\n    \n    def __init__(self, judge_llm):\n        self.judge = judge_llm\n    \n    async def evaluate_response(\n        self,\n        query: str,\n        response: str,\n        criteria: List[str]\n    ) -> dict:\n        \"\"\"Evaluate response against criteria.\"\"\"\n        prompt = f\"\"\"\n        Evaluate this response:\n        \n        Query: {query}\n        Response: {response}\n        \n        Criteria: {criteria}\n        \n        Rate each criterion from 1-5 and explain.\n        Output JSON: {{\"criterion\": score, \"explanation\": \"...\"}}\n        \"\"\"\n        \n        result = await self.judge.generate(prompt)\n        return json.loads(result)\n    \n    async def compare_responses(\n        self,\n        query: str,\n        response_a: str,\n        response_b: str\n    ) -> dict:\n        \"\"\"Compare two responses.\"\"\"\n        prompt = f\"\"\"\n        Compare these responses:\n        \n        Query: {query}\n        Response A: {response_a}\n        Response B: {response_b}\n        \n        Which is better? Output JSON:\n        {{\n          \"winner\": \"A\" or \"B\",\n          \"confidence\": 0-1,\n          \"reasoning\": \"...\"\n        }}\n        \"\"\"\n        \n        result = await self.judge.generate(prompt)\n        return json.loads(result)\n```\n\n### Benchmarking\n\n```python\nclass Benchmark:\n    \"\"\"\n    Run benchmarks on AI system.\n    \"\"\"\n    \n    def __init__(self, system):\n        self.system = system\n        self.results = {}\n    \n    async def run_all(self):\n        \"\"\"Run all benchmarks.\"\"\"\n        for name, benchmark in self.benchmarks.items():\n            self.results[name] = await self.run_benchmark(benchmark)\n    \n    async def run_benchmark(self, benchmark: dict) -> dict:\n        \"\"\"Run single benchmark.\"\"\"\n        results = []\n        \n        for sample in benchmark[\"samples\"]:\n            start_time = time.time()\n            \n            output = await self.system.process(sample[\"input\"])\n            \n            elapsed = time.time() - start_time\n            \n            results.append({\n                \"input\": sample[\"input\"],\n                \"output\": output,\n                \"expected\": sample.get(\"expected\"),\n                \"latency\": elapsed,\n                \"metrics\": self.calculate_metrics(output, sample)\n            })\n        \n        return self.aggregate_results(results)\n    \n    def generate_report(self) -> str:\n        \"\"\"Generate benchmark report.\"\"\"\n        report = [\"# Benchmark Report\\n\"]\n        \n        for name, results in self.results.items():\n            report.append(f\"## {name}\")\n            report.append(f\"- Accuracy: {results['accuracy']:.2%}\")\n            report.append(f\"- Avg Latency: {results['avg_latency']:.2f}s\")\n            report.append(f\"- P95 Latency: {results['p95_latency']:.2f}s\")\n            report.append(\"\")\n        \n        return \"\\n\".join(report)\n```\n\n### Hands-on Exercise\n\n1. Implement retrieval evaluation metrics\n2. Build LLM-as-judge evaluation\n3. Create a benchmark suite\n4. Generate evaluation reports"
        },
        {
          "title": "Monitoring and Observability",
          "content": "## AI System Monitoring\n\n### What to Monitor\n\n1. **Performance**: Latency, throughput\n2. **Quality**: Accuracy, relevance\n3. **Cost**: Token usage, API costs\n4. **Errors**: Failure rates, types\n5. **Drift**: Model degradation\n\n### Metrics Collection\n\n```python\nclass MetricsCollector:\n    \"\"\"\n    Collect AI system metrics.\n    \"\"\"\n    \n    def __init__(self):\n        self.metrics = defaultdict(list)\n        self.counters = defaultdict(int)\n    \n    def record_latency(\n        self,\n        operation: str,\n        latency_ms: float\n    ):\n        \"\"\"Record operation latency.\"\"\"\n        self.metrics[f\"latency_{operation}\"].append(latency_ms)\n    \n    def record_tokens(\n        self,\n        model: str,\n        input_tokens: int,\n        output_tokens: int\n    ):\n        \"\"\"Record token usage.\"\"\"\n        self.counters[f\"tokens_{model}_input\"] += input_tokens\n        self.counters[f\"tokens_{model}_output\"] += output_tokens\n    \n    def record_error(\n        self,\n        operation: str,\n        error_type: str\n    ):\n        \"\"\"Record error occurrence.\"\"\"\n        self.counters[f\"error_{operation}_{error_type}\"] += 1\n    \n    def get_stats(self) -> dict:\n        \"\"\"Get aggregated statistics.\"\"\"\n        stats = {}\n        \n        for key, values in self.metrics.items():\n            stats[f\"{key}_avg\"] = np.mean(values)\n            stats[f\"{key}_p50\"] = np.percentile(values, 50)\n            stats[f\"{key}_p95\"] = np.percentile(values, 95)\n            stats[f\"{key}_p99\"] = np.percentile(values, 99)\n        \n        stats.update(self.counters)\n        return stats\n```\n\n### Logging\n\n```python\nimport structlog\n\nclass AILogger:\n    \"\"\"\n    Structured logging for AI operations.\n    \"\"\"\n    \n    def __init__(self):\n        self.logger = structlog.get_logger()\n    \n    def log_request(\n        self,\n        request_id: str,\n        prompt: str,\n        model: str,\n        parameters: dict\n    ):\n        \"\"\"Log LLM request.\"\"\"\n        self.logger.info(\n            \"llm_request\",\n            request_id=request_id,\n            model=model,\n            prompt_tokens=count_tokens(prompt),\n            temperature=parameters.get(\"temperature\"),\n            max_tokens=parameters.get(\"max_tokens\")\n        )\n    \n    def log_response(\n        self,\n        request_id: str,\n        response: str,\n        latency_ms: float,\n        tokens_used: int\n    ):\n        \"\"\"Log LLM response.\"\"\"\n        self.logger.info(\n            \"llm_response\",\n            request_id=request_id,\n            response_tokens=tokens_used,\n            latency_ms=latency_ms\n        )\n    \n    def log_tool_call(\n        self,\n        request_id: str,\n        tool_name: str,\n        arguments: dict,\n        result: dict\n    ):\n        \"\"\"Log tool execution.\"\"\"\n        self.logger.info(\n            \"tool_call\",\n            request_id=request_id,\n            tool_name=tool_name,\n            success=result.get(\"success\", False)\n        )\n```\n\n### Distributed Tracing\n\n```python\nfrom opentelemetry import trace\n\nclass TracedAIOperation:\n    \"\"\"\n    Add tracing to AI operations.\n    \"\"\"\n    \n    tracer = trace.get_tracer(__name__)\n    \n    @classmethod\n    async def trace_llm_call(\n        cls,\n        model: str,\n        prompt: str,\n        **kwargs\n    ):\n        \"\"\"Trace LLM call.\"\"\"\n        with cls.tracer.start_as_current_span(\n            \"llm_call\",\n            attributes={\n                \"model\": model,\n                \"prompt_tokens\": count_tokens(prompt)\n            }\n        ) as span:\n            try:\n                result = await llm.generate(prompt, **kwargs)\n                span.set_attribute(\n                    \"response_tokens\",\n                    count_tokens(result)\n                )\n                span.set_attribute(\"success\", True)\n                return result\n            except Exception as e:\n                span.set_attribute(\"success\", False)\n                span.set_attribute(\"error\", str(e))\n                raise\n    \n    @classmethod\n    async def trace_rag_query(\n        cls,\n        query: str,\n        **kwargs\n    ):\n        \"\"\"Trace RAG query.\"\"\"\n        with cls.tracer.start_as_current_span(\"rag_query\") as span:\n            # Retrieve\n            with cls.tracer.start_as_current_span(\"retrieve\"):\n                context = await retriever.retrieve(query)\n            \n            span.set_attribute(\"context_size\", len(context))\n            \n            # Generate\n            with cls.tracer.start_as_current_span(\"generate\"):\n                response = await generator.generate(query, context)\n            \n            return response\n```\n\n### Alerting\n\n```python\nclass AIAlertManager:\n    \"\"\"\n    Alert on AI system issues.\n    \"\"\"\n    \n    def __init__(self, thresholds: dict):\n        self.thresholds = thresholds\n        self.notifiers = []\n    \n    def check_latency(self, operation: str, latency_ms: float):\n        \"\"\"Check latency threshold.\"\"\"\n        threshold = self.thresholds.get(f\"latency_{operation}\", 5000)\n        \n        if latency_ms > threshold:\n            self.alert(\n                severity=\"warning\",\n                message=f\"High latency for {operation}: {latency_ms}ms\",\n                metric=\"latency\",\n                value=latency_ms,\n                threshold=threshold\n            )\n    \n    def check_error_rate(self, operation: str, rate: float):\n        \"\"\"Check error rate threshold.\"\"\"\n        threshold = self.thresholds.get(f\"error_rate_{operation}\", 0.05)\n        \n        if rate > threshold:\n            self.alert(\n                severity=\"critical\",\n                message=f\"High error rate for {operation}: {rate:.2%}\",\n                metric=\"error_rate\",\n                value=rate,\n                threshold=threshold\n            )\n    \n    def alert(self, severity: str, message: str, **context):\n        \"\"\"Send alert.\"\"\"\n        for notifier in self.notifiers:\n            notifier.send(severity, message, context)\n```\n\n### Dashboards\n\n```python\nclass MetricsDashboard:\n    \"\"\"\n    Generate metrics dashboard data.\n    \"\"\"\n    \n    def __init__(self, collector: MetricsCollector):\n        self.collector = collector\n    \n    def get_summary(self, time_range: str = \"1h\") -> dict:\n        \"\"\"Get metrics summary.\"\"\"\n        return {\n            \"requests\": {\n                \"total\": self.collector.get_count(\"requests\"),\n                \"success_rate\": self.collector.get_rate(\"success\"),\n            },\n            \"latency\": {\n                \"avg\": self.collector.get_percentile(\"latency\", 50),\n                \"p95\": self.collector.get_percentile(\"latency\", 95),\n                \"p99\": self.collector.get_percentile(\"latency\", 99),\n            },\n            \"tokens\": {\n                \"input\": self.collector.get_total(\"tokens_input\"),\n                \"output\": self.collector.get_total(\"tokens_output\"),\n                \"cost\": self.calculate_cost(),\n            },\n            \"errors\": {\n                \"total\": self.collector.get_count(\"errors\"),\n                \"by_type\": self.collector.get_grouped(\"error_type\"),\n            }\n        }\n    \n    def get_timeseries(\n        self,\n        metric: str,\n        granularity: str = \"1m\"\n    ) -> List[dict]:\n        \"\"\"Get time series data.\"\"\"\n        return self.collector.get_timeseries(metric, granularity)\n```\n\n### Drift Detection\n\n```python\nclass DriftDetector:\n    \"\"\"\n    Detect model drift.\n    \"\"\"\n    \n    def __init__(self, baseline: dict, window_size: int = 100):\n        self.baseline = baseline\n        self.window_size = window_size\n        self.recent_metrics = []\n    \n    def add_sample(self, metrics: dict):\n        \"\"\"Add sample metrics.\"\"\"\n        self.recent_metrics.append(metrics)\n        \n        if len(self.recent_metrics) > self.window_size:\n            self.recent_metrics.pop(0)\n    \n    def detect_drift(self) -> List[dict]:\n        \"\"\"Detect drift from baseline.\"\"\"\n        drifts = []\n        \n        for metric_name, baseline_value in self.baseline.items():\n            current_value = np.mean([\n                m[metric_name] for m in self.recent_metrics\n            ])\n            \n            # Statistical test\n            if abs(current_value - baseline_value) > 2 * np.std([\n                m[metric_name] for m in self.recent_metrics\n            ]):\n                drifts.append({\n                    \"metric\": metric_name,\n                    \"baseline\": baseline_value,\n                    \"current\": current_value,\n                    \"severity\": self.calculate_severity(\n                        baseline_value,\n                        current_value\n                    )\n                })\n        \n        return drifts\n```\n\n### Hands-on Exercise\n\n1. Build metrics collection system\n2. Implement distributed tracing\n3. Create alert rules for common issues\n4. Build a monitoring dashboard"
        },
        {
          "title": "Week 7 Project: AI Quality Assurance System",
          "content": "## Project: Build an AI QA System\n\n### Objective\nCreate a comprehensive quality assurance system for AI applications.\n\n### Requirements\n\n#### 1. Test Framework\n```python\nclass AITestFramework:\n    \"\"\"\n    Framework for testing AI systems.\n    \"\"\"\n    \n    def __init__(self):\n        self.test_suites = {}\n        self.fixtures = {}\n    \n    def add_suite(self, name: str, tests: List[Test]):\n        pass\n    \n    async def run_all(self) -> TestReport:\n        pass\n```\n\n#### 2. Evaluation Pipeline\n```python\nclass EvaluationPipeline:\n    \"\"\"\n    Pipeline for evaluating AI systems.\n    \"\"\"\n    \n    def __init__(self):\n        self.evaluators = []\n    \n    def add_evaluator(self, evaluator: Evaluator):\n        pass\n    \n    async def evaluate(self, system: AISystem) -> EvaluationReport:\n        pass\n```\n\n#### 3. Monitoring System\n```python\nclass AIMonitor:\n    \"\"\"\n    Monitor AI systems in production.\n    \"\"\"\n    \n    def __init__(self):\n        self.metrics = MetricsCollector()\n        self.alerts = AlertManager()\n        self.tracer = Tracer()\n    \n    def track_request(self, request: Request) -> RequestContext:\n        pass\n    \n    def record_response(self, context: RequestContext, response: Response):\n        pass\n```\n\n### Project Structure\n\n```\nai_qa_system/\n├── testing/\n│   ├── __init__.py\n│   ├── framework.py\n│   ├── fixtures.py\n│   └── assertions.py\n├── evaluation/\n│   ├── __init__.py\n│   ├── metrics.py\n│   ├── evaluators/\n│   │   ├── retrieval.py\n│   │   ├── generation.py\n│   │   └── agent.py\n│   └── datasets.py\n├── monitoring/\n│   ├── __init__.py\n│   ├── collector.py\n│   ├── tracing.py\n│   ├── alerts.py\n│   └── dashboard.py\n├── analysis/\n│   ├── __init__.py\n│   ├── drift.py\n│   ├── reports.py\n│   └── visualization.py\n├── api/\n│   ├── __init__.py\n│   └── routes.py\n└── cli.py\n```\n\n### Features\n\n1. **Automated Testing**\n   - Unit tests for AI components\n   - Integration tests\n   - Property-based tests\n   - Regression tests\n\n2. **Evaluation Suite**\n   - Retrieval metrics (P@K, R@K, MRR, NDCG)\n   - Generation metrics (BLEU, ROUGE, semantic)\n   - Agent metrics (success rate, tool accuracy)\n   - LLM-as-judge evaluation\n\n3. **Monitoring**\n   - Real-time metrics\n   - Distributed tracing\n   - Error tracking\n   - Performance analytics\n\n4. **Alerting**\n   - Latency alerts\n   - Error rate alerts\n   - Drift detection alerts\n   - Cost alerts\n\n5. **Reporting**\n   - Daily/weekly reports\n   - Benchmark comparisons\n   - Trend analysis\n   - Custom dashboards\n\n### CLI Commands\n\n```bash\n# Run tests\nqa test run --suite integration\nqa test run --all\n\n# Evaluate system\nqa eval run --benchmark qa_benchmark\nqa eval compare --baseline v1.0 --current v1.1\n\n# Monitor\nqa monitor start --port 8080\nqa monitor dashboard\n\n# Reports\nqa report generate --period weekly\nqa report export --format pdf\n```\n\n### Dashboard Features\n\n```\n┌────────────────────────────────────────────┐\n│          AI System Dashboard               │\n├────────────────────────────────────────────┤\n│                                            │\n│  Requests/min: 125    Success: 98.5%       │\n│  Avg Latency: 245ms   P95: 890ms           │\n│                                            │\n│  ┌──────────┐  ┌──────────┐               │\n│  │ Latency  │  │  Errors  │               │\n│  │   [chart]│  │  [chart] │               │\n│  └──────────┘  └──────────┘               │\n│                                            │\n│  ┌────────────────────────────────────┐   │\n│  │         Quality Metrics            │   │\n│  │  Retrieval: 0.87  Gen: 0.82        │   │\n│  │  Agent: 0.91      Overall: 0.86    │   │\n│  └────────────────────────────────────┘   │\n│                                            │\n│  Recent Alerts:                            │\n│  ⚠ High latency (2m ago)                  │\n│  ✓ Recovered (1m ago)                     │\n└────────────────────────────────────────────┘\n```\n\n### Deliverables\n\n1. **Working QA system** with all features\n2. **Test suites** for common AI patterns\n3. **Evaluation benchmarks**\n4. **Monitoring dashboard**\n5. **CLI tool**\n6. **Documentation**\n\n### Evaluation Criteria\n\n- Feature completeness (30%)\n- Code quality (25%)\n- Usability (20%)\n- Documentation (15%)\n- Tests (10%)\n\n### Extension Ideas\n\n- A/B testing framework\n- Automated regression detection\n- Performance profiling\n- Cost optimization suggestions\n- Integration with CI/CD"
        }
      ]
    },
    {
      "week": 8,
      "title": "Deploying AI to Production",
      "description": "Take AI systems from prototype to production. Learn deployment strategies, scaling, cost optimization, and maintenance best practices.",
      "lessons": [
        {
          "title": "Production Deployment Strategies",
          "content": "## Deployment Considerations\n\n### Production Requirements\n\n1. **Reliability**: High availability, error handling\n2. **Scalability**: Handle varying loads\n3. **Performance**: Low latency requirements\n4. **Cost**: Optimize resource usage\n5. **Security**: Protect data and access\n\n### Architecture Patterns\n\n#### 1. API Service Pattern\n```yaml\n# Architecture\n┌─────────────┐\n│   Client    │\n└──────┬──────┘\n       │\n┌──────▼──────┐\n│   Gateway   │  (Rate limiting, Auth)\n└──────┬──────┘\n       │\n┌──────▼──────┐\n│ AI Service  │  (LLM calls, Processing)\n└──────┬──────┘\n       │\n┌──────▼──────┐\n│   Vector DB │  (Embeddings, Retrieval)\n└─────────────┘\n```\n\n```python\nfrom fastapi import FastAPI, HTTPException\nfrom pydantic import BaseModel\n\napp = FastAPI()\n\nclass QueryRequest(BaseModel):\n    query: str\n    context: dict = None\n    options: dict = None\n\nclass QueryResponse(BaseModel):\n    answer: str\n    sources: list\n    confidence: float\n    latency_ms: float\n\n@app.post(\"/api/query\", response_model=QueryResponse)\nasync def query(request: QueryRequest):\n    start_time = time.time()\n    \n    try:\n        result = await ai_system.process(\n            request.query,\n            context=request.context,\n            options=request.options\n        )\n        \n        return QueryResponse(\n            answer=result.answer,\n            sources=result.sources,\n            confidence=result.confidence,\n            latency_ms=(time.time() - start_time) * 1000\n        )\n    except Exception as e:\n        logger.error(f\"Query failed: {e}\")\n        raise HTTPException(status_code=500, detail=str(e))\n\n@app.get(\"/health\")\nasync def health_check():\n    return {\n        \"status\": \"healthy\",\n        \"components\": {\n            \"llm\": await check_llm(),\n            \"database\": await check_db(),\n            \"cache\": await check_cache()\n        }\n    }\n```\n\n#### 2. Async Processing Pattern\n```python\nfrom celery import Celery\n\napp = Celery('ai_tasks', broker='redis://localhost:6379')\n\n@app.task\ndef process_document(document_id: str):\n    \"\"\"Process document asynchronously.\"\"\"\n    document = fetch_document(document_id)\n    \n    # Chunk and embed\n    chunks = chunk_document(document)\n    embeddings = embed_chunks(chunks)\n    \n    # Store in vector DB\n    vector_db.store(document_id, embeddings)\n    \n    return {\"status\": \"completed\", \"chunks\": len(chunks)}\n\n@app.task\ndef batch_query(queries: List[str]):\n    \"\"\"Process multiple queries.\"\"\"\n    results = []\n    for query in queries:\n        result = ai_system.query(query)\n        results.append(result)\n    return results\n\n# API endpoint\n@app.post(\"/api/documents/process\")\nasync def process_doc(document_id: str):\n    task = process_document.delay(document_id)\n    return {\"task_id\": task.id, \"status\": \"processing\"}\n\n@app.get(\"/api/tasks/{task_id}\")\nasync def get_task_status(task_id: str):\n    task = app.AsyncResult(task_id)\n    return {\n        \"status\": task.state,\n        \"result\": task.result if task.ready() else None\n    }\n```\n\n### Containerization\n\n```dockerfile\n# Dockerfile\nFROM python:3.11-slim\n\nWORKDIR /app\n\n# Install dependencies\nCOPY requirements.txt .\nRUN pip install --no-cache-dir -r requirements.txt\n\n# Copy application\nCOPY . .\n\n# Set environment\nENV PYTHONUNBUFFERED=1\nENV MODEL_NAME=gpt-4\n\n# Run\nCMD [\"uvicorn\", \"main:app\", \"--host\", \"0.0.0.0\", \"--port\", \"8000\"]\n```\n\n```yaml\n# docker-compose.yml\nversion: '3.8'\n\nservices:\n  api:\n    build: .\n    ports:\n      - \"8000:8000\"\n    environment:\n      - OPENAI_API_KEY=${OPENAI_API_KEY}\n      - DATABASE_URL=postgresql://db:5432/ai_db\n    depends_on:\n      - db\n      - redis\n    \n  db:\n    image: postgres:15\n    volumes:\n      - postgres_data:/var/lib/postgresql/data\n    environment:\n      - POSTGRES_DB=ai_db\n      - POSTGRES_PASSWORD=secret\n  \n  redis:\n    image: redis:7-alpine\n    \n  worker:\n    build: .\n    command: celery -A tasks worker --loglevel=info\n    depends_on:\n      - redis\n\nvolumes:\n  postgres_data:\n```\n\n### Kubernetes Deployment\n\n```yaml\n# deployment.yaml\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: ai-service\nspec:\n  replicas: 3\n  selector:\n    matchLabels:\n      app: ai-service\n  template:\n    metadata:\n      labels:\n        app: ai-service\n    spec:\n      containers:\n      - name: api\n        image: ai-service:latest\n        ports:\n        - containerPort: 8000\n        env:\n        - name: OPENAI_API_KEY\n          valueFrom:\n            secretKeyRef:\n              name: ai-secrets\n              key: openai-key\n        resources:\n          requests:\n            memory: \"512Mi\"\n            cpu: \"500m\"\n          limits:\n            memory: \"2Gi\"\n            cpu: \"2000m\"\n        livenessProbe:\n          httpGet:\n            path: /health\n            port: 8000\n          initialDelaySeconds: 30\n          periodSeconds: 10\n        readinessProbe:\n          httpGet:\n            path: /ready\n            port: 8000\n          initialDelaySeconds: 5\n          periodSeconds: 5\n---\napiVersion: v1\nkind: Service\nmetadata:\n  name: ai-service\nspec:\n  selector:\n    app: ai-service\n  ports:\n  - port: 80\n    targetPort: 8000\n  type: LoadBalancer\n---\napiVersion: autoscaling/v2\nkind: HorizontalPodAutoscaler\nmetadata:\n  name: ai-service-hpa\nspec:\n  scaleTargetRef:\n    apiVersion: apps/v1\n    kind: Deployment\n    name: ai-service\n  minReplicas: 2\n  maxReplicas: 10\n  metrics:\n  - type: Resource\n    resource:\n      name: cpu\n      target:\n        type: Utilization\n        averageUtilization: 70\n```\n\n### Configuration Management\n\n```python\nfrom pydantic import BaseSettings\n\nclass Settings(BaseSettings):\n    # API Settings\n    api_title: str = \"AI Service\"\n    api_version: str = \"1.0.0\"\n    \n    # LLM Settings\n    openai_api_key: str\n    default_model: str = \"gpt-4-turbo-preview\"\n    max_tokens: int = 2000\n    temperature: float = 0.7\n    \n    # Database Settings\n    database_url: str\n    vector_db_url: str\n    \n    # Redis Settings\n    redis_url: str = \"redis://localhost:6379\"\n    \n    # Performance\n    max_concurrent_requests: int = 100\n    request_timeout: int = 30\n    \n    # Monitoring\n    enable_metrics: bool = True\n    log_level: str = \"INFO\"\n    \n    class Config:\n        env_file = \".env\"\n\nsettings = Settings()\n```\n\n### Hands-on Exercise\n\n1. Create FastAPI service for AI application\n2. Add Docker and docker-compose setup\n3. Deploy to Kubernetes (minikube)\n4. Configure autoscaling"
        },
        {
          "title": "Scaling and Performance",
          "content": "## Scaling AI Systems\n\n### Performance Bottlenecks\n\n1. **LLM Latency**: API call times\n2. **Embedding Generation**: Vector creation\n3. **Vector Search**: Retrieval speed\n4. **Context Processing**: Token handling\n\n### Optimization Strategies\n\n#### 1. Caching\n```python\nclass AIResponseCache:\n    \"\"\"\n    Multi-layer caching for AI responses.\n    \"\"\"\n    \n    def __init__(self):\n        self.memory_cache = TTLCache(maxsize=1000, ttl=300)\n        self.redis_cache = RedisCache()\n        self.embedding_cache = EmbeddingCache()\n    \n    async def get(self, query: str) -> Optional[dict]:\n        # Check memory cache\n        cache_key = self._hash(query)\n        if cache_key in self.memory_cache:\n            return self.memory_cache[cache_key]\n        \n        # Check Redis\n        cached = await self.redis_cache.get(cache_key)\n        if cached:\n            self.memory_cache[cache_key] = cached\n            return cached\n        \n        return None\n    \n    async def set(self, query: str, response: dict, ttl: int = 300):\n        cache_key = self._hash(query)\n        \n        # Store in both layers\n        self.memory_cache[cache_key] = response\n        await self.redis_cache.set(cache_key, response, ttl)\n    \n    def _hash(self, query: str) -> str:\n        return hashlib.sha256(query.encode()).hexdigest()\n\n# Semantic caching\nclass SemanticCache:\n    \"\"\"\n    Cache based on semantic similarity.\n    \"\"\"\n    \n    def __init__(self, threshold: float = 0.95):\n        self.threshold = threshold\n        self.cache_db = VectorDB()\n    \n    async def get_similar(self, query: str) -> Optional[dict]:\n        query_embedding = await embed(query)\n        \n        results = self.cache_db.search(\n            query_embedding,\n            k=1,\n            min_score=self.threshold\n        )\n        \n        if results:\n            return results[0].metadata['response']\n        \n        return None\n    \n    async def add(self, query: str, response: dict):\n        embedding = await embed(query)\n        \n        self.cache_db.upsert(\n            id=self._hash(query),\n            embedding=embedding,\n            metadata={'query': query, 'response': response}\n        )\n```\n\n#### 2. Request Batching\n```python\nclass BatchProcessor:\n    \"\"\"\n    Batch multiple requests together.\n    \"\"\"\n    \n    def __init__(self, batch_size: int = 10, timeout: float = 0.1):\n        self.batch_size = batch_size\n        self.timeout = timeout\n        self.queue = asyncio.Queue()\n    \n    async def add_request(self, request: Request) -> Response:\n        future = asyncio.Future()\n        await self.queue.put((request, future))\n        return await future\n    \n    async def process_batches(self):\n        while True:\n            batch = []\n            futures = []\n            \n            # Collect batch\n            try:\n                while len(batch) < self.batch_size:\n                    request, future = await asyncio.wait_for(\n                        self.queue.get(),\n                        timeout=self.timeout\n                    )\n                    batch.append(request)\n                    futures.append(future)\n            except asyncio.TimeoutError:\n                pass\n            \n            if batch:\n                # Process batch\n                results = await self.process_batch(batch)\n                \n                # Resolve futures\n                for future, result in zip(futures, results):\n                    future.set_result(result)\n    \n    async def process_batch(self, batch: List[Request]) -> List[Response]:\n        # Use batch API if available\n        return await llm.batch_generate([r.prompt for r in batch])\n```\n\n#### 3. Connection Pooling\n```python\nclass ConnectionPool:\n    \"\"\"\n    Pool connections to external services.\n    \"\"\"\n    \n    def __init__(\n        self,\n        max_connections: int = 100,\n        max_keepalive: int = 20\n    ):\n        self limits = httpx.Limits(\n            max_connections=max_connections,\n            max_keepalive_connections=max_keepalive\n        )\n        self.client = httpx.AsyncClient(limits=self.limits)\n    \n    async def request(self, method: str, url: str, **kwargs):\n        return await self.client.request(method, url, **kwargs)\n```\n\n#### 4. Load Balancing\n```python\nclass LoadBalancer:\n    \"\"\"\n    Distribute load across multiple endpoints.\n    \"\"\"\n    \n    def __init__(self, endpoints: List[str]):\n        self.endpoints = endpoints\n        self.current = 0\n        self.health = {ep: True for ep in endpoints}\n    \n    def get_next(self) -> str:\n        \"\"\"Round-robin with health check.\"\"\"\n        attempts = 0\n        \n        while attempts < len(self.endpoints):\n            endpoint = self.endpoints[self.current]\n            self.current = (self.current + 1) % len(self.endpoints)\n            \n            if self.health[endpoint]:\n                return endpoint\n            \n            attempts += 1\n        \n        raise Exception(\"No healthy endpoints\")\n    \n    def mark_unhealthy(self, endpoint: str):\n        self.health[endpoint] = False\n    \n    def mark_healthy(self, endpoint: str):\n        self.health[endpoint] = True\n```\n\n### Performance Monitoring\n\n```python\nclass PerformanceMonitor:\n    \"\"\"\n    Monitor and optimize performance.\n    \"\"\"\n    \n    def __init__(self):\n        self.metrics = defaultdict(list)\n    \n    @contextmanager\n    def track(self, operation: str):\n        start = time.time()\n        yield\n        elapsed = time.time() - start\n        self.metrics[operation].append(elapsed)\n    \n    def get_stats(self, operation: str) -> dict:\n        times = self.metrics[operation]\n        \n        return {\n            \"count\": len(times),\n            \"avg\": np.mean(times),\n            \"p50\": np.percentile(times, 50),\n            \"p95\": np.percentile(times, 95),\n            \"p99\": np.percentile(times, 99)\n        }\n    \n    def identify_bottlenecks(self) -> List[dict]:\n        bottlenecks = []\n        \n        for op, times in self.metrics.items():\n            p95 = np.percentile(times, 95)\n            if p95 > 1.0:  # > 1 second\n                bottlenecks.append({\n                    \"operation\": op,\n                    \"p95\": p95,\n                    \"severity\": \"high\" if p95 > 5 else \"medium\"\n                })\n        \n        return sorted(bottlenecks, key=lambda x: x[\"p95\"], reverse=True)\n```\n\n### Auto-scaling\n\n```python\nclass AutoScaler:\n    \"\"\"\n    Auto-scale based on metrics.\n    \"\"\"\n    \n    def __init__(\n        self,\n        min_instances: int = 2,\n        max_instances: int = 10,\n        target_latency: float = 1.0\n    ):\n        self.min = min_instances\n        self.max = max_instances\n        self.target = target_latency\n        self.current_instances = min_instances\n    \n    def evaluate(self, metrics: dict) -> str:\n        \"\"\"Evaluate scaling decision.\"\"\"\n        latency = metrics.get(\"latency_p95\", 0)\n        cpu = metrics.get(\"cpu_percent\", 0)\n        \n        # Scale up conditions\n        if latency > self.target * 1.5 or cpu > 80:\n            if self.current_instances < self.max:\n                self.current_instances += 1\n                return f\"scale_up:{self.current_instances}\"\n        \n        # Scale down conditions\n        elif latency < self.target * 0.5 and cpu < 30:\n            if self.current_instances > self.min:\n                self.current_instances -= 1\n                return f\"scale_down:{self.current_instances}\"\n        \n        return \"no_change\"\n```\n\n### Hands-on Exercise\n\n1. Implement multi-layer caching\n2. Add request batching\n3. Build performance monitoring\n4. Create auto-scaling logic"
        },
        {
          "title": "Cost Optimization",
          "content": "## Understanding AI Costs\n\n### Cost Components\n\n1. **LLM API Costs**: Per token charges\n2. **Embedding Costs**: Vector generation\n3. **Vector DB Costs**: Storage and queries\n4. **Compute**: Server resources\n5. **Network**: Data transfer\n\n### Token Cost Optimization\n\n```python\nclass CostOptimizer:\n    \"\"\"\n    Optimize token usage and costs.\n    \"\"\"\n    \n    def __init__(self):\n        self.model_costs = {\n            \"gpt-4\": {\"input\": 0.03, \"output\": 0.06},  # per 1K tokens\n            \"gpt-3.5-turbo\": {\"input\": 0.0015, \"output\": 0.002},\n            \"claude-3-opus\": {\"input\": 0.015, \"output\": 0.075},\n            \"claude-3-sonnet\": {\"input\": 0.003, \"output\": 0.015}\n        }\n    \n    def estimate_cost(\n        self,\n        model: str,\n        input_tokens: int,\n        output_tokens: int\n    ) -> float:\n        \"\"\"Estimate cost for request.\"\"\"\n        costs = self.model_costs[model]\n        return (\n            (input_tokens / 1000) * costs[\"input\"] +\n            (output_tokens / 1000) * costs[\"output\"]\n        )\n    \n    def select_optimal_model(\n        self,\n        task: str,\n        complexity: str = \"medium\",\n        budget: float = None\n    ) -> str:\n        \"\"\"Select best model for budget.\"\"\"\n        if complexity == \"high\":\n            return \"gpt-4\"\n        elif complexity == \"medium\":\n            return \"gpt-3.5-turbo\"\n        else:\n            return \"gpt-3.5-turbo\"\n    \n    def optimize_prompt(self, prompt: str) -> str:\n        \"\"\"Reduce prompt token count.\"\"\"\n        # Remove redundant phrases\n        optimized = prompt\n        \n        # Compress examples\n        # Remove unnecessary context\n        # Use shorter synonyms\n        \n        return optimized\n```\n\n### Caching for Cost Savings\n\n```python\nclass CostAwareCache:\n    \"\"\"\n    Cache with cost-based decisions.\n    \"\"\"\n    \n    def __init__(self, min_savings: float = 0.001):\n        self.cache = {}\n        self.min_savings = min_savings\n        self.savings = 0\n    \n    async def get_or_compute(\n        self,\n        key: str,\n        compute_fn: Callable,\n        estimated_cost: float\n    ) -> tuple:\n        \"\"\"Get from cache or compute.\"\"\"\n        if key in self.cache:\n            self.savings += estimated_cost\n            return self.cache[key], True\n        \n        result = await compute_fn()\n        \n        # Cache if cost > threshold\n        if estimated_cost > self.min_savings:\n            self.cache[key] = result\n        \n        return result, False\n    \n    def get_savings_report(self) -> dict:\n        return {\n            \"total_savings\": self.savings,\n            \"cache_hits\": len([k for k in self.cache if k]),\n            \"estimated_requests_saved\": self.savings / 0.01\n        }\n```\n\n### Model Selection Strategy\n\n```python\nclass SmartModelSelector:\n    \"\"\"\n    Select models based on task and cost.\n    \"\"\"\n    \n    def __init__(self):\n        self.task_model_map = {\n            \"simple_qa\": \"gpt-3.5-turbo\",\n            \"code_generation\": \"gpt-4\",\n            \"summarization\": \"gpt-3.5-turbo\",\n            \"complex_reasoning\": \"gpt-4\",\n            \"creative_writing\": \"gpt-4\",\n            \"classification\": \"gpt-3.5-turbo\"\n        }\n    \n    def select(\n        self,\n        task: str,\n        requirements: dict = None\n    ) -> str:\n        \"\"\"Select optimal model.\"\"\"\n        base_model = self.task_model_map.get(task, \"gpt-3.5-turbo\")\n        \n        # Override based on requirements\n        if requirements:\n            if requirements.get(\"needs_best_reasoning\"):\n                return \"gpt-4\"\n            if requirements.get(\"low_latency\"):\n                return \"gpt-3.5-turbo\"\n            if requirements.get(\"long_context\"):\n                return \"claude-3-opus\"\n        \n        return base_model\n```\n\n### Budget Management\n\n```python\nclass BudgetManager:\n    \"\"\"\n    Manage and enforce budgets.\n    \"\"\"\n    \n    def __init__(\n        self,\n        daily_budget: float = 100.0,\n        monthly_budget: float = 2000.0\n    ):\n        self.daily_budget = daily_budget\n        self.monthly_budget = monthly_budget\n        self.spend = {\"daily\": 0, \"monthly\": 0}\n    \n    def can_proceed(self, estimated_cost: float) -> bool:\n        \"\"\"Check if within budget.\"\"\"\n        return (\n            self.spend[\"daily\"] + estimated_cost <= self.daily_budget\n            and self.spend[\"monthly\"] + estimated_cost <= self.monthly_budget\n        )\n    \n    def record_spend(self, amount: float):\n        \"\"\"Record spending.\"\"\"\n        self.spend[\"daily\"] += amount\n        self.spend[\"monthly\"] += amount\n    \n    def get_status(self) -> dict:\n        return {\n            \"daily\": {\n                \"spent\": self.spend[\"daily\"],\n                \"budget\": self.daily_budget,\n                \"remaining\": self.daily_budget - self.spend[\"daily\"],\n                \"percent_used\": self.spend[\"daily\"] / self.daily_budget * 100\n            },\n            \"monthly\": {\n                \"spent\": self.spend[\"monthly\"],\n                \"budget\": self.monthly_budget,\n                \"remaining\": self.monthly_budget - self.spend[\"monthly\"],\n                \"percent_used\": self.spend[\"monthly\"] / self.monthly_budget * 100\n            }\n        }\n```\n\n### Cost Monitoring\n\n```python\nclass CostMonitor:\n    \"\"\"\n    Monitor and alert on costs.\n    \"\"\"\n    \n    def __init__(self, alert_thresholds: dict):\n        self.thresholds = alert_thresholds\n        self.spending = defaultdict(float)\n    \n    def track_request(\n        self,\n        model: str,\n        input_tokens: int,\n        output_tokens: int\n    ):\n        \"\"\"Track request cost.\"\"\"\n        cost = self.calculate_cost(model, input_tokens, output_tokens)\n        \n        self.spending[model] += cost\n        self.spending[\"total\"] += cost\n        \n        # Check thresholds\n        self.check_thresholds(model, cost)\n    \n    def check_thresholds(self, model: str, cost: float):\n        \"\"\"Check if thresholds exceeded.\"\"\"\n        hourly_threshold = self.thresholds.get(\"hourly\", 10)\n        \n        if self.spending[\"hourly\"] > hourly_threshold:\n            self.alert(\n                \"hourly_threshold_exceeded\",\n                {\"spent\": self.spending[\"hourly\"], \"threshold\": hourly_threshold}\n            )\n    \n    def get_report(self, period: str = \"day\") -> dict:\n        \"\"\"Generate cost report.\"\"\"\n        return {\n            \"period\": period,\n            \"total\": self.spending[\"total\"],\n            \"by_model\": dict(self.spending),\n            \"projected_monthly\": self.project_monthly()\n        }\n```\n\n### Hands-on Exercise\n\n1. Build cost tracking system\n2. Implement smart model selection\n3. Create budget enforcement\n4. Add cost monitoring and alerts"
        },
        {
          "title": "Week 8 Project: Production AI Platform",
          "content": "## Project: Deploy a Production AI Platform\n\n### Objective\nDeploy a complete, production-ready AI platform with all best practices.\n\n### Requirements\n\n#### 1. Core Services\n```yaml\nservices:\n  - API Gateway (Authentication, Rate Limiting)\n  - AI Processing Service\n  - Vector Database Service\n  - Cache Service (Redis)\n  - Queue Service (Celery/RabbitMQ)\n  - Monitoring Service\n```\n\n#### 2. Production Features\n\n**Reliability**\n- Health checks\n- Circuit breakers\n- Retry logic\n- Graceful degradation\n\n**Scalability**\n- Horizontal scaling\n- Load balancing\n- Auto-scaling rules\n- Queue-based processing\n\n**Security**\n- API authentication\n- Rate limiting\n- Input validation\n- Secrets management\n\n**Observability**\n- Structured logging\n- Metrics collection\n- Distributed tracing\n- Alerting\n\n### Architecture\n\n```\n                    ┌─────────────┐\n                    │   Clients   │\n                    └──────┬──────┘\n                           │\n                    ┌──────▼──────┐\n                    │ API Gateway │\n                    │  - Auth     │\n                    │  - Rate     │\n                    │  - Route    │\n                    └──────┬──────┘\n                           │\n          ┌────────────────┼────────────────┐\n          │                │                │\n    ┌─────▼─────┐   ┌─────▼─────┐   ┌─────▼─────┐\n    │  AI API   │   │  AI API   │   │  AI API   │\n    │  Instance │   │  Instance │   │  Instance │\n    └─────┬─────┘   └─────┬─────┘   └─────┬─────┘\n          │                │                │\n          └────────────────┼────────────────┘\n                           │\n    ┌──────────────────────┼──────────────────────┐\n    │                      │                      │\n┌───▼───┐           ┌─────▼─────┐          ┌─────▼─────┐\n│ Redis │           │ Vector DB │          │  Postgres │\n└───────┘           └───────────┘          └───────────┘\n```\n\n### Project Structure\n\n```\nproduction_ai_platform/\n├── services/\n│   ├── api/\n│   │   ├── app/\n│   │   │   ├── main.py\n│   │   │   ├── routes/\n│   │   │   ├── middleware/\n│   │   │   └── config.py\n│   │   ├── Dockerfile\n│   │   └── requirements.txt\n│   ├── worker/\n│   │   ├── tasks/\n│   │   ├── Dockerfile\n│   │   └── requirements.txt\n│   └── gateway/\n│       ├── nginx.conf\n│       └── Dockerfile\n├── infrastructure/\n│   ├── kubernetes/\n│   │   ├── deployments/\n│   │   ├── services/\n│   │   ├── configmaps/\n│   │   └── secrets/\n│   ├── terraform/\n│   │   ├── main.tf\n│   │   ├── variables.tf\n│   │   └── outputs.tf\n│   └── docker-compose.yml\n├── monitoring/\n│   ├── prometheus/\n│   │   └── prometheus.yml\n│   ├── grafana/\n│   │   └── dashboards/\n│   └── alertmanager/\n│       └── alerts.yml\n├── scripts/\n│   ├── deploy.sh\n│   ├── scale.sh\n│   └── monitor.sh\n├── tests/\n│   ├── integration/\n│   ├── load/\n│   └── e2e/\n└── docs/\n    ├── api.md\n    ├── deployment.md\n    └── runbook.md\n```\n\n### Deployment Checklist\n\n**Pre-deployment**\n- [ ] All tests passing\n- [ ] Environment variables configured\n- [ ] Secrets stored securely\n- [ ] Database migrations ready\n- [ ] Monitoring configured\n\n**Deployment**\n- [ ] Blue-green deployment\n- [ ] Health checks passing\n- [ ] Load balancer updated\n- [ ] DNS switched\n- [ ] Alerts active\n\n**Post-deployment**\n- [ ] Metrics normal\n- [ ] Error rates low\n- [ ] Latency acceptable\n- [ ] Cost tracking active\n\n### API Endpoints\n\n```python\n# Core endpoints\nPOST   /api/v1/query              # Query AI\nPOST   /api/v1/documents          # Upload document\nGET    /api/v1/documents/{id}     # Get document\nDELETE /api/v1/documents/{id}     # Delete document\nPOST   /api/v1/embeddings         # Generate embeddings\nGET    /api/v1/health             # Health check\nGET    /api/v1/metrics            # Prometheus metrics\n\n# Admin endpoints\nGET    /api/v1/admin/stats        # System stats\nGET    /api/v1/admin/costs        # Cost report\nPOST   /api/v1/admin/cache/clear  # Clear cache\n```\n\n### Monitoring Dashboard\n\n```\n┌───────────────────────────────────────────────────────┐\n│            AI Platform Dashboard                      │\n├───────────────────────────────────────────────────────┤\n│                                                       │\n│  Traffic        Success Rate     Latency P95          │\n│  1.2k/min       99.2%            245ms                │\n│                                                       │n│  ┌────────────┐  ┌────────────┐  ┌────────────┐     │\n│  │   Traffic   │  │   Errors   │  │   Latency  │     │\n│  │   [graph]   │  │   [graph]  │  │   [graph]  │     │\n│  └────────────┘  └────────────┘  └────────────┘     │\n│                                                       │\n│  Resources          Cost Today    Cost Month         │\n│  CPU: 45%           $45.20        $1,234             │\n│  Memory: 62%                                         │\n│                                                       │\n│  Models Used:                                        │\n│  - GPT-4: 12k tokens ($0.72)                         │\n│  - GPT-3.5: 89k tokens ($0.18)                       │\n│                                                       │\n│  Alerts: None                                        │\n└───────────────────────────────────────────────────────┘\n```\n\n### Deliverables\n\n1. **Complete platform** deployed\n2. **Kubernetes manifests**\n3. **Monitoring setup** (Prometheus + Grafana)\n4. **Documentation**:\n   - API documentation\n   - Deployment guide\n   - Runbook\n5. **Tests**: Integration + load tests\n6. **CI/CD pipeline**\n\n### Evaluation Criteria\n\n- Production readiness (30%)\n- Architecture quality (25%)\n- Documentation (20%)\n- Monitoring (15%)\n- Testing (10%)\n\n### Extension Ideas\n\n- Multi-region deployment\n- Disaster recovery\n- A/B testing infrastructure\n- Feature flags\n- Self-healing systems\n```
      ]
    }
  ],
  "estimated_duration": "8 weeks",
  "total_lessons": 32,
  "skills_acquired": [
    "Prompt engineering",
    "LLM API integration",
    "RAG system development",
    "AI agent architecture",
    "Function calling and tools",
    "API integration",
    "Testing and evaluation",
    "Production deployment"
  ],
  "projects": [
    "Prompt Engineering Toolkit",
    "Multi-Provider LLM Gateway",
    "Production RAG System",
    "Autonomous Research Agent",
    "Tool-Using Agent",
    "AI Integration Hub",
    "AI Quality Assurance System",
    "Production AI Platform"
  ]
}
