# 2026-03-31 Session Log

## AI Professor - Deployment Fixed ✅

### Problem:
- Site was 404 (DEPLOYMENT_NOT_FOUND) after previous session's failed Vercel config
- Git push blocked — fine-grained PAT couldn't write to other users' repos
- Vercel couldn't see nested `projects/ai-professor` as Root Directory
- Multiple failed approaches over ~4 hours (previous session went till 2am)

### Solution:
1. Created new repo `bryanbenjaminleong/ai-professor-app`
2. Bryan added ashdevs999-pixel as collaborator
3. Generated **Classic PAT** (fine-grained couldn't cross accounts) — old token `REDACTED`
4. Pushed workspace (Next.js app at `projects/ai-professor/`)
5. Vercel: imported repo, set Root Directory to `projects/ai-professor`
6. Added all env vars (Supabase, OpenAI, Redis, etc.)
7. Build succeeded: 44 pages, all routes working

### New Live URL:
- **https://ai-professor-app.vercel.app**

### Key Lessons:
- Fine-grained PATs can ONLY access repos owned by the token's account (not collaborator repos)
- Classic PATs with `repo` scope work for cross-account access
- Vercel Root Directory selector only shows top-level folders — but Settings lets you type nested paths
- Vercel needs env vars BEFORE deploy or pages crash silently

### Tokens Updated:
- gh CLI auth: switched to new classic PAT
- Git remote `bryan-app`: configured with token in URL
